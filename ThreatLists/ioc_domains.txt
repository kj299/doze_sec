# Last verified by doze_sec: 2026-06-06 10:20:39
# SENTINEL-X CTI -- C2 Domain Patterns
# Updated: 2026-03-29 | Source: MDDR 2024-2025, CISA, VirusTotal, Mandiant
# Format: domain fragment per line (matched against DNS cache via findstr /i)
# NOTE: These are PATTERN fragments, not full defanged IOCs
#
# === KNOWN C2 INFRASTRUCTURE PATTERNS ===
update-service.cloud
cdn-update.cloud
secure-update.cloud
windowsupdate.cc
msupdate.top
ms-update.cloud
defender-update.cloud
office-update.cloud
onedrive-sync.cloud
teams-update.cloud
#
# === COBALT STRIKE MALLEABLE C2 PATTERNS ===
cdn-static.cloud
cloudfront-api.cloud
azureedge-cdn.cloud
akamai-static.cloud
#
# === DGA / SUSPICIOUS TLD PATTERNS ===
# NOTE: Bare TLD patterns (.top, .xyz, .club, .buzz, .surf, .icu,
# .cam, .rest, .monster) were removed. These are findstr substring
# patterns, so .top matches "laptop" and "desktop", .rest matches
# REST API documentation cache entries, etc. .top alone has 4M+
# registered legit domains. DGA detection requires entropy analysis
# or rare-TLD frequency baselining, not bare suffix matching.
#
# === TUNNELING SERVICES (legitimate but abused) ===
ngrok.io
trycloudflare.com
serveo.net
localhost.run
bore.digital
localtunnel.me
#
# === DNS-OVER-HTTPS ABUSE ===
# NOTE: cloudflare-dns.com/dns-query and dns.google/resolve were
# removed -- these are first-party DoH endpoints from the two
# largest DNS providers, used by default in Firefox and Chrome
# in many regions. doh.dns.sb retained as a less-common third-party
# DoH endpoint that's a stronger signal in enterprise environments.
doh.dns.sb
#
# === SCATTERED SPIDER / SOCIAL ENG INFRA ===
# NOTE: Bare suffix patterns (-okta.com, -sso.com, -login.com,
# -helpdesk.com, -vpn.com, -mfa.com) were removed. findstr substring
# matching makes "myapp-login.com" indistinguishable from a phishing
# domain "paypal-login.com". Typosquat detection requires DNS
# distance analysis against known-legit corporate domains, which is
# out of scope for this script.
