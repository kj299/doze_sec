name: windows-smoke

# Real-Windows smoke tests. The lint workflow runs on Linux/pwsh and the
# author develops on Linux, so neither can exercise cmd.exe end-to-end,
# Windows PowerShell 5.1 runtime behavior, real WMI/CIM, findstr, or detect
# a runtime HANG. These jobs close that gap by running on a genuine Windows
# runner (Windows Server 2022 + built-in Windows PowerShell 5.1).
#
# Job 1 (helpers-ps51): parses every tools/*.ps1 with the 5.1 language parser
# and executes the read-only ones against real WMI/CIM/scheduled-tasks/
# Authenticode/Get-FileHash -- catching 5.1-only and WMI issues a Linux/pwsh
# box cannot see. Includes a 20,000-char-line select_lines.ps1 case (the
# exact shape that hangs findstr).
#
# Job 2 (full-run): runs doze_sec.bat end-to-end under a hard timeout. A hang
# (e.g. the Section 4 findstr-on-long-lines regression) makes the job exceed
# its timeout and FAIL, instead of silently shipping. -dev bypasses the
# Server-edition OS gate; -sdu/-nosrp skip the network threat-list pull and
# the restore point.

# TRIGGER ECONOMICS: this repository is private, so Actions minutes are
# metered, and these five windows-latest jobs bill at 2x with 20-35 minute
# timeouts. Running them on every PR push exhausted the month's quota
# mid-cycle (2026-08). The first response removed the suite from PRs entirely
# -- which meant a bug fix or feature got only the Linux lint before merge and
# the Windows suite only after landing on main. That is backwards: the diet
# must never cost coverage of a functional change. So the suite runs on any
# PR that touches functional code (the bats, tools/, tests/, threat lists,
# the workflows) and skips only docs-only PRs; the concurrency rule below
# stops a superseded push from billing a second full suite.
on:
  push:
    branches: [main]
  pull_request:
    paths:
      - 'doze_sec.bat'
      - 'doze_sec_noAdmin.bat'
      - 'tools/**'
      - 'tests/**'
      - 'ThreatLists/**'
      - '.github/workflows/**'
  # Manual kick. Without this there is no way to re-run these checks except by
  # pushing a commit -- which is exactly the wrong tool when the checks failed
  # for a reason that has nothing to do with the diff (a runner outage, an
  # expired token, a transient registry fetch). Keep it: the day you need it,
  # adding it requires the push you were trying to avoid.
  workflow_dispatch:

# A superseded run's minutes are pure waste at 2x billing.
concurrency:
  group: windows-smoke-${{ github.ref }}
  cancel-in-progress: true

jobs:
  helpers-ps51:
    name: "tools/*.ps1 on Windows PowerShell 5.1 (real WMI/CIM)"
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      # Note: every step ends with `exit 0`. The helpers legitimately return a
      # non-zero exit code (e.g. select_lines.ps1 returns 1 for "no match",
      # mirroring findstr); GitHub's powershell shell would otherwise propagate
      # that leaked $LASTEXITCODE as a step failure. Assertion failures still
      # `exit 1` before reaching the final `exit 0`, and ErrorActionPreference
      # = Stop fails the step on any thrown error.
      - name: Parse every helper with the Windows PowerShell 5.1 parser
        shell: powershell
        run: |
          $ErrorActionPreference = 'Stop'
          $bad = @()
          Get-ChildItem tools\*.ps1, tests\*.ps1 | ForEach-Object {
            $errs = $null
            [void][System.Management.Automation.Language.Parser]::ParseFile($_.FullName, [ref]$null, [ref]$errs)
            if ($errs) { $bad += "$($_.Name): $($errs[0].Message)" }
          }
          if ($bad) { $bad | ForEach-Object { Write-Host "PARSE ERROR  $_" }; exit 1 }
          Write-Host "All tools/*.ps1 + tests/*.ps1 parse clean under Windows PowerShell $($PSVersionTable.PSVersion)"
          exit 0

      - name: scheduled_tasks_full.ps1 (real Get-ScheduledTask + Authenticode)
        shell: powershell
        run: |
          $ErrorActionPreference = 'Stop'
          foreach ($m in 'Inventory','Suspicious','System') {
            .\tools\scheduled_tasks_full.ps1 -Mode $m | Out-Null
            if ($LASTEXITCODE -gt 1) { Write-Host "scheduled_tasks_full -Mode $m exit $LASTEXITCODE"; exit 1 }
          }
          Write-Host "scheduled_tasks_full OK (all three modes)"
          exit 0

      - name: browser_extensions.ps1 (real profile enumeration)
        shell: powershell
        run: |
          $ErrorActionPreference = 'Stop'
          .\tools\browser_extensions.ps1 | Out-Null
          Write-Host "browser_extensions OK"
          exit 0

      - name: select_lines.ps1 incl. a 20,000-char line (findstr would hang)
        shell: powershell
        run: |
          $ErrorActionPreference = 'Stop'
          $f = Join-Path $env:TEMP 'dz_ci_proc.tmp'
          'brave.exe  1  C:\Users\x\AppData\Local\BraveSoftware\Brave-Browser\Application\brave.exe' | Set-Content $f
          ('giant.exe  2  C:\Windows\giant.exe ' + ('x' * 20000)) | Add-Content $f
          'mshta.exe  3  C:\Windows\System32\mshta.exe' | Add-Content $f
          .\tools\select_lines.ps1 -Path $f "\AppData\" | Out-Null
          if ($LASTEXITCODE -ne 0) { Write-Host "expected AppData match (exit 0), got $LASTEXITCODE"; exit 1 }
          .\tools\select_lines.ps1 -Path $f "ScreenConnect" "AnyDesk" | Out-Null
          if ($LASTEXITCODE -ne 1) { Write-Host "expected no RMM match (exit 1), got $LASTEXITCODE"; exit 1 }
          Write-Host "select_lines OK (matched through a 20KB line, no hang)"
          exit 0

      - name: top_findings.ps1 + report_format.ps1 on a sample report
        shell: powershell
        run: |
          $ErrorActionPreference = 'Stop'
          $r = Join-Path $env:TEMP 'dz_ci_report.txt'
          $lines = @(
            '===================================================================='
            ' [6/18] SCHEDULED TASKS'
            '===================================================================='
            '--- CRITICAL: Tasks with Actions in Suspicious Paths ---'
            '[CRITICAL] 1 scheduled task(s) with actions in suspicious locations: EvilTask'
            '===================================================================='
            ' [1/18] SYSTEM IDENTITY AND PATCH LEVEL'
            '===================================================================='
            '[OK] placeholder line'
          )
          $lines | Set-Content -LiteralPath $r -Encoding UTF8
          .\tools\top_findings.ps1 -Report $r
          if (-not (Select-String -LiteralPath $r -Pattern 'TOP FINDINGS' -Quiet)) { Write-Host "top_findings did not prepend its block"; exit 1 }
          .\tools\report_format.ps1 -Report $r | Out-Null
          Write-Host "top_findings + report_format OK"
          exit 0

      - name: report_html.ps1 surfaces a Findings Index with section links
        shell: powershell
        run: |
          $ErrorActionPreference = 'Stop'
          $r = Join-Path $env:TEMP 'dz_ci_html_report.txt'
          $h = Join-Path $env:TEMP 'dz_ci_html_report.html'
          $lines = @(
            '===================================================================='
            ' [18/18] CTI-DRIVEN IOC SWEEP'
            '===================================================================='
            '[CRITICAL] Known malware staging files found: C:\x\evil.exe'
            '######################################################################'
            '##  SECURITY AUDIT SUMMARY  --  CITEST'
            '##  ACTION REQUIRED  --  1 CRITICAL  /  2 WARNING  /  5 PASSED'
            '######################################################################'
            '  --- CTI IOC SWEEP  (Section 18 - SENTINEL-X) -----------------------'
            '  [!! CRITICAL !!]  CTI IOC sweep: 1 category matches found'
            '                     Fix: Review Section 18 for specific IOC matches.'
            '  --- SYSTEM HARDENING  (Section 13) ---------------------------------'
            '  [  WARNING   ]  BitLocker OFF for C:'
            '  [    OK      ]  UAC on with prompt'
            '  NEXT STEPS -- CRITICAL issues found:'
          )
          $lines | Set-Content -LiteralPath $r -Encoding UTF8
          .\tools\report_html.ps1 -Report $r -HtmlPath $h
          if (-not (Test-Path $h)) { Write-Host 'report_html produced no HTML'; exit 1 }
          $html = Get-Content -LiteralPath $h -Raw
          if ($html -notmatch 'Findings Index') { Write-Host 'no Findings Index panel'; exit 1 }
          if ($html -notmatch 'CTI IOC sweep: 1 category matches found') { Write-Host 'critical finding not surfaced'; exit 1 }
          if ($html -notmatch 'href="#sec18"') { Write-Host 'critical not linked to its section'; exit 1 }
          if ($html -notmatch '<div class="card card-crit"><h3>1</h3>') { Write-Host 'dashboard crit count not taken from verdict line'; exit 1 }

          # The HTML page is what doze_sec.bat opens in the browser. It used to
          # drop every line before the first section banner -- the report
          # header, the whole Tier 0 READ THIS FIRST block, and the table of
          # contents -- because $inPre starts false and the preamble is pure
          # prose. Findings told the reader to "read the READ THIS FIRST
          # section at the top of this report" before remediating (it can alert
          # an abusive partner and destroys evidence) and that section was not
          # there. Assert the real preamble survives, and that it is NOT buried
          # in a collapsed <details>.
          $r2 = Join-Path $env:TEMP 'dz_ci_html_preamble.txt'
          $h2 = Join-Path $env:TEMP 'dz_ci_html_preamble.html'
          $pre = .\tools\report_safety.ps1 -Mode Preamble
          $pre + @(
            '  TABLE OF CONTENTS'
            '===================================================================='
            ' [13/18] SYSTEM HARDENING'
            '===================================================================='
            '[OK] placeholder'
          ) | Set-Content -LiteralPath $r2 -Encoding UTF8
          .\tools\report_html.ps1 -Report $r2 -HtmlPath $h2
          $html2 = Get-Content -LiteralPath $h2 -Raw
          foreach ($must in @('READ THIS FIRST', 'accessnow.org/help', 'stopstalkerware.org', 'PRESERVE EVIDENCE', 'TABLE OF CONTENTS')) {
            if ($html2 -notmatch [regex]::Escape($must)) { Write-Host "preamble content missing from HTML: $must"; exit 1 }
          }
          if ($html2 -notmatch 'id="readfirst"') { Write-Host 'no readfirst panel in the HTML'; exit 1 }
          # The panel must be reachable without expanding anything: everything
          # from the panel open tag to its close must contain no <details>.
          $panel = [regex]::Match($html2, 'id="readfirst"[\s\S]*?</div>')
          if (-not $panel.Success) { Write-Host 'readfirst panel not delimited'; exit 1 }
          if ($panel.Value -match '<details') { Write-Host 'READ THIS FIRST is hidden inside a <details> -- it must be visible without interaction'; exit 1 }
          Remove-Item $r2, $h2 -Force -EA SilentlyContinue
          Write-Host 'report_html OK (Findings Index, section link, verdict-based counts, visible READ THIS FIRST)'
          exit 0

      - name: ioc_hash_check.ps1 (real Get-FileHash, capped)
        shell: powershell
        run: |
          $ErrorActionPreference = 'Stop'
          .\tools\ioc_hash_check.ps1 -IocFile ThreatLists\ioc_hashes.txt -MaxFiles 3 | Out-Null
          Write-Host "ioc_hash_check OK"
          exit 0

      - name: startup_eval.ps1 (Startup folder + AppCert DLL evaluation)
        shell: powershell
        run: |
          $ErrorActionPreference = 'Stop'
          $md = Join-Path $env:TEMP ("dz_se_ci_{0}" -f $PID)
          New-Item -ItemType Directory -Path $md -Force | Out-Null
          # Clean runner: must produce no CRITICAL and write no marker.
          # NB: -match/-notmatch on an ARRAY are FILTERS, not booleans. Join to
          # a single string before testing, or `-notmatch` returns every
          # non-matching line (truthy) and the check always fires.
          $out = (.\tools\startup_eval.ps1 -MarkerDir $md) -join "`n"
          Write-Host $out
          if ($out -match '\[CRITICAL\]') { throw "startup_eval flagged CRITICAL on a clean runner (false positive)" }
          if (Test-Path (Join-Path $md 'dz_startup_folder.txt')) {
            throw ("startup marker written on a clean runner: {0}" -f (Get-Content (Join-Path $md 'dz_startup_folder.txt')))
          }
          if (Test-Path (Join-Path $md 'dz_appcert.txt')) {
            throw ("appcert marker written on a clean runner: {0}" -f (Get-Content (Join-Path $md 'dz_appcert.txt')))
          }
          # Plant an AppCert DLL pointing at a missing staging-path file: must
          # be CRITICAL and must write the marker.
          $ac = 'HKLM:\SYSTEM\CurrentControlSet\Control\Session Manager\AppCertDlls'
          if (-not (Test-Path $ac)) { New-Item -Path $ac -Force | Out-Null }
          Set-ItemProperty -Path $ac -Name 'dz_ci_probe' -Value 'C:\Windows\Temp\dz_ci_probe.dll' -Force
          try {
            $out2 = (.\tools\startup_eval.ps1 -MarkerDir $md) -join "`n"
            if ($out2 -notmatch 'dz_ci_probe') { throw "planted AppCert DLL not reported" }
            $m = Join-Path $md 'dz_appcert.txt'
            if (-not (Test-Path $m)) { throw "planted AppCert DLL wrote no marker" }
            if ((Get-Content $m).Trim() -ne 'CRITICAL') { throw ("expected CRITICAL marker, got {0}" -f (Get-Content $m)) }
          } finally {
            Remove-ItemProperty -Path $ac -Name 'dz_ci_probe' -EA SilentlyContinue
            Remove-Item $md -Recurse -Force -EA SilentlyContinue
          }
          Write-Host "startup_eval.ps1 OK"
          exit 0

      - name: ledger.ps1 (finding-record append / summarize / section contract)
        shell: powershell
        run: |
          $ErrorActionPreference = 'Stop'
          $L = Join-Path $env:TEMP ("dz_ledger_ci_{0}.txt" -f $PID)
          Remove-Item $L -Force -EA SilentlyContinue
          .\tools\ledger.ps1 -Mode Append -Path $L -Severity WARNING  -Section 9  -Code T1562.001 -Message 'ASR rules not in Block mode'
          .\tools\ledger.ps1 -Mode Append -Path $L -Severity CRITICAL -Section 13 -Message 'IFEO hijack on sethc.exe'
          .\tools\ledger.ps1 -Mode Append -Path $L -Severity WARNING  -Section 9  -Message 'second Section 9 finding'
          $sum = .\tools\ledger.ps1 -Mode Summarize -Path $L
          # NB: loop var must not be $l -- PowerShell variable names are
          # case-insensitive, so $l would clobber the ledger path $L.
          $h = @{}; foreach ($line in $sum) { if ($line -match '^(\w+)=(.+)$') { $h[$Matches[1]] = $Matches[2] } }
          if ($h.TOTAL    -ne '3')        { throw "TOTAL expected 3, got $($h.TOTAL)" }
          if ($h.CRITICAL -ne '1')        { throw "CRITICAL expected 1, got $($h.CRITICAL)" }
          if ($h.WARNING  -ne '2')        { throw "WARNING expected 2, got $($h.WARNING)" }
          if ($h.MAXSEV   -ne 'CRITICAL') { throw "MAXSEV expected CRITICAL, got $($h.MAXSEV)" }
          $s9 = "$(.\tools\ledger.ps1 -Mode Section -Path $L -Section 9)".Trim()
          if ($s9 -ne '2') { throw "Section 9 count expected 2, got $s9" }
          $s99 = "$(.\tools\ledger.ps1 -Mode Section -Path $L -Section 99)".Trim()
          if ($s99 -ne '0') { throw "Section 99 count expected 0, got $s99" }
          # Separator/newline injection in a message must not corrupt the record.
          .\tools\ledger.ps1 -Mode Append -Path $L -Severity WARNING -Section 8 -Message 'pipe|inside|the message'
          $s8 = "$(.\tools\ledger.ps1 -Mode Section -Path $L -Section 8)".Trim()
          if ($s8 -ne '1') { throw "Section 8 count expected 1 after pipe-injection, got $s8" }
          Remove-Item $L -Force -EA SilentlyContinue
          Write-Host "ledger.ps1 OK"
          exit 0

      - name: persistence_extra.ps1 (netsh / print / BITS / profile / time-provider)
        shell: powershell
        run: |
          $ErrorActionPreference = 'Stop'
          $md = Join-Path $env:TEMP ("dz_pe_ci_{0}" -f $PID)
          New-Item -ItemType Directory -Path $md -Force | Out-Null
          $markers = @('dz_netsh.txt','dz_printproc.txt','dz_portmon.txt','dz_bits.txt','dz_psprofile.txt','dz_timeprov.txt')
          # Clean runner: a stock Windows image registers netsh helpers, print
          # processors, port monitors and time providers, and they are all
          # Microsoft-signed -- so any CRITICAL here is a false positive in the
          # signature gate, which is exactly the risk this check exists to catch.
          # NB: -match on an ARRAY is a FILTER, not a boolean -- join first.
          $out = (.\tools\persistence_extra.ps1 -MarkerDir $md) -join "`n"
          Write-Host $out
          if ($out -match '\[CRITICAL\]') { throw "persistence_extra flagged CRITICAL on a clean runner (false positive)" }
          foreach ($m in $markers) {
            if (Test-Path (Join-Path $md $m)) { throw ("{0} written on a clean runner: {1}" -f $m, (Get-Content (Join-Path $md $m))) }
          }
          # The detection harness plants the time-provider and PowerShell-profile
          # vectors end-to-end (proving the bat marker wiring). The three print/
          # netsh vectors are planted HERE instead: a bogus netsh helper makes
          # netsh.exe emit load errors, which would contaminate the firewall and
          # portproxy cases in the full-audit harness. Isolated per-vector plants
          # keep that blast radius inside this step.
          $netsh = 'HKLM:\SOFTWARE\Microsoft\Netsh'
          $mon   = 'HKLM:\SYSTEM\CurrentControlSet\Control\Print\Monitors\dz_ci_probe'
          $pproc = 'HKLM:\SYSTEM\CurrentControlSet\Control\Print\Environments\Windows x64\Print Processors\dz_ci_probe'
          try {
            Set-ItemProperty -Path $netsh -Name 'dz_ci_probe' -Value 'dz_ci_probe_missing.dll' -Force
            New-Item -Path $mon -Force | Out-Null
            Set-ItemProperty -Path $mon -Name 'Driver' -Value 'dz_ci_probe_missing.dll' -Force
            New-Item -Path $pproc -Force | Out-Null
            Set-ItemProperty -Path $pproc -Name 'Driver' -Value 'dz_ci_probe_missing.dll' -Force
            $out2 = (.\tools\persistence_extra.ps1 -MarkerDir $md) -join "`n"
            Write-Host $out2
            foreach ($pair in @(
              @{ Rx = "Netsh helper 'dz_ci_probe'";    Marker = 'dz_netsh.txt' },
              @{ Rx = "Port monitor 'dz_ci_probe'";    Marker = 'dz_portmon.txt' },
              @{ Rx = "Print processor 'dz_ci_probe'"; Marker = 'dz_printproc.txt' }
            )) {
              if ($out2 -notmatch [regex]::Escape($pair.Rx)) { throw ("planted vector not reported: {0}" -f $pair.Rx) }
              $mp = Join-Path $md $pair.Marker
              if (-not (Test-Path $mp)) { throw ("planted vector wrote no marker: {0}" -f $pair.Marker) }
              if ((Get-Content $mp).Trim() -ne 'CRITICAL') { throw ("{0}: expected CRITICAL, got {1}" -f $pair.Marker, (Get-Content $mp)) }
            }

            # A BRACKETED KEY NAME MUST NOT MAKE AN ENTRY DISAPPEAR. The value
            # reads used -Path, which treats the attacker-controlled subkey name
            # as a WILDCARD -- so a port monitor called "Std [TCP] Mon" matched
            # nothing, -EA SilentlyContinue hid the error, the entry was dropped
            # without being counted, and the section still reported every
            # registered monitor as validly Microsoft-signed. spoolsv loads port
            # monitors as SYSTEM at boot, so this is a real evasion.
            $wild = 'HKLM:\SYSTEM\CurrentControlSet\Control\Print\Monitors\dz_ci [TCP] probe'
            New-Item -Path $wild -Force | Out-Null
            Set-ItemProperty -LiteralPath $wild -Name 'Driver' -Value 'dz_ci_probe_missing.dll' -Force
            $md4 = Join-Path $md 'wild'
            New-Item -ItemType Directory -Path $md4 -Force | Out-Null
            $out3 = (.\tools\persistence_extra.ps1 -MarkerDir $md4) -join "`n"
            Write-Host ($out3 -split "`n" | Where-Object { $_ -match 'dz_ci \[TCP\] probe|unreadable driver value|port monitor' })
            if ($out3 -notmatch 'dz_ci \[TCP\] probe') { throw 'a port monitor with brackets in its key name vanished from the scan' }
            $mp4 = Join-Path $md4 'dz_portmon.txt'
            if (-not (Test-Path $mp4)) { throw 'bracketed port monitor wrote no marker -- it was silently skipped' }
            Remove-Item -LiteralPath $wild -Recurse -Force -EA SilentlyContinue
          } finally {
            Remove-Item -LiteralPath 'HKLM:\SYSTEM\CurrentControlSet\Control\Print\Monitors\dz_ci [TCP] probe' -Recurse -Force -EA SilentlyContinue
            Remove-ItemProperty -Path $netsh -Name 'dz_ci_probe' -EA SilentlyContinue
            Remove-Item -LiteralPath $mon   -Recurse -Force -EA SilentlyContinue
            Remove-Item -LiteralPath $pproc -Recurse -Force -EA SilentlyContinue
            Remove-Item $md -Recurse -Force -EA SilentlyContinue
          }
          Write-Host "persistence_extra.ps1 OK (incl. bracketed-key-name evasion)"
          exit 0

      - name: audit_policy_check.ps1 (auditing visibility; locale-independent cmdline signal)
        shell: powershell
        run: |
          $ErrorActionPreference = 'Stop'
          $md = Join-Path $env:TEMP ("dz_ap_ci_{0}" -f $PID)
          New-Item -ItemType Directory -Path $md -Force | Out-Null
          try {
            $out = (.\tools\audit_policy_check.ps1 -MarkerDir $md) -join "`n"
            Write-Host $out
            # The runner's real cmdline-logging key state must match what the
            # tool prints -- proves the definitive signal is read correctly.
            $k = 'HKLM:\SOFTWARE\Microsoft\Windows\CurrentVersion\Policies\System\Audit'
            $on = ((Get-ItemProperty -Path $k -Name 'ProcessCreationIncludeCmdLine_Enabled' -EA SilentlyContinue).ProcessCreationIncludeCmdLine_Enabled -eq 1)
            if ($on -and $out -notmatch 'command-line logging is ENABLED') { throw 'cmdline key ON but tool did not report ENABLED' }
            if (-not $on -and $out -notmatch 'command-line logging is DISABLED') { throw 'cmdline key OFF but tool did not report DISABLED' }

            # Ask WINDOWS what each configured GUID actually is, rather than
            # trusting the constant in the source. A retrospective found this
            # row querying {0CCE9217} (Audit_Logon_AccountLockout) while
            # reporting on "Audit Policy Change (4719)" -- so disabling 4719,
            # the exact T1562.002 action the row exists to catch, still printed
            # "auditing is ON". Three of the four GUIDs were right, which is
            # precisely why nobody noticed. auditpol /list resolves GUID -> name
            # on the real OS; the runner is English so the names are stable.
            $expect = @{
              '{0CCE922B-69AE-11D9-BED3-505054503030}' = 'Process Creation'
              '{0CCE9215-69AE-11D9-BED3-505054503030}' = 'Logon'
              '{0CCE9235-69AE-11D9-BED3-505054503030}' = 'User Account Management'
              '{0CCE922F-69AE-11D9-BED3-505054503030}' = 'Audit Policy Change'
            }
            # NB: /list /subcategory: takes a CATEGORY name or *, never a GUID
            # (passing one returns 0x57 ERROR_INVALID_PARAMETER). Enumerate all
            # subcategories once with /v, which prints "<name>  <guid>", and
            # build the mapping from that. The native command writes to stderr,
            # so relax EAP around it or PowerShell turns that into a
            # terminating NativeCommandError.
            $eap = $ErrorActionPreference; $ErrorActionPreference = 'Continue'
            $list = & auditpol /list /subcategory:* /v 2>&1
            $ErrorActionPreference = $eap
            $map = @{}
            foreach ($ln in $list) {
              $m = [regex]::Match([string]$ln, '^\s*(\S.*?)\s+(\{[0-9A-Fa-f]{8}-[0-9A-Fa-f-]+\})\s*$')
              if ($m.Success) { $map[$m.Groups[2].Value.ToUpper()] = $m.Groups[1].Value.Trim() }
            }
            # Guard the guard: an empty or tiny map would make every comparison
            # below vacuously true.
            if ($map.Count -lt 10) { throw "auditpol /list returned only $($map.Count) subcategory mappings -- the GUID check cannot verify anything" }
            $src = Get-Content .\tools\audit_policy_check.ps1 -Raw
            foreach ($g in $expect.Keys) {
              if ($src -notmatch [regex]::Escape($g)) { throw "audit_policy_check.ps1 no longer queries $g ($($expect[$g]))" }
              $actual = $map[$g.ToUpper()]
              if (-not $actual) { throw "auditpol does not report a subcategory with GUID $g" }
              if ($actual -ne $expect[$g]) { throw "GUID $g is '$actual' on this OS, but audit_policy_check.ps1 labels it '$($expect[$g])'" }
              Write-Host ("  {0} -> {1}  (confirmed by auditpol)" -f $g, $actual)
            }
          } finally { Remove-Item $md -Recurse -Force -EA SilentlyContinue }
          Write-Host "audit_policy_check.ps1 OK (incl. GUID-to-subcategory verification against the live OS)"
          exit 0

      - name: driver_audit.ps1 (BYOVD by hash + unsigned catch-all; renamed cannot evade)
        shell: powershell
        run: |
          $ErrorActionPreference = 'Stop'
          $md = Join-Path $env:TEMP ("dz_drv_ci_{0}" -f $PID)
          New-Item -ItemType Directory -Path $md -Force | Out-Null
          # Clean runner: every loaded/inbox driver is Microsoft- or vendor-signed,
          # so no marker should be written. A CRITICAL here is a signature-gate bug.
          $out = (.\tools\driver_audit.ps1 -MarkerDir $md) -join "`n"
          Write-Host ($out -split "`n" | Select-Object -First 40)
          if (Test-Path (Join-Path $md 'dz_driver.txt')) {
            throw ("driver marker written on a clean runner: {0}" -f (Get-Content (Join-Path $md 'dz_driver.txt')))
          }
          # Plant an unsigned "driver" in a drop location: the signature catch-all
          # (not a name/hash match) must flag it -- this is what defeats renaming.
          $plant = 'C:\Users\Public\dz_ci_probe_driver.sys'
          Set-Content -LiteralPath $plant -Value 'MZ not a real driver' -Encoding ASCII
          try {
            $out2 = (.\tools\driver_audit.ps1 -MarkerDir $md) -join "`n"
            Write-Host ($out2 -split "`n" | Where-Object { $_ -match 'dz_ci_probe_driver' })
            if ($out2 -notmatch 'dz_ci_probe_driver\.sys') { throw 'planted unsigned driver not reported' }
            $m = Join-Path $md 'dz_driver.txt'
            if (-not (Test-Path $m)) { throw 'planted unsigned driver wrote no marker' }

            # ATTACK SURFACE vs EVIDENCE OF COMPROMISE. A known-abusable driver
            # name that is VALIDLY SIGNED and in the normal drivers directory is
            # most likely legitimate software (VirtualBox ships vboxdrv.sys,
            # Sysinternals ships procexp152.sys). The old rule set CRITICAL on
            # the name alone and skipped the signature check entirely, so a
            # developer machine got "CRITICAL findings present -- review NOW"
            # and exit code 8. Copy a real signed Microsoft driver to a
            # known-abusable NAME in each location and check the severities.
            $srcSigned = Join-Path $env:SystemRoot 'System32\drivers\tcpip.sys'
            $inPlace = Join-Path $env:SystemRoot 'System32\drivers\gdrv.sys'
            $staged  = 'C:\Users\Public\gdrv.sys'
            $md2 = Join-Path $env:TEMP ("dz_drv_ci2_{0}" -f $PID)
            New-Item -ItemType Directory -Path $md2 -Force | Out-Null
            try {
              Copy-Item -LiteralPath $srcSigned -Destination $inPlace -Force
              $o3 = (.\tools\driver_audit.ps1 -MarkerDir $md2) -join "`n"
              $l3 = (($o3 -split "`n") | Where-Object { $_ -match 'gdrv\.sys' } | Select-Object -First 1)
              if (-not $l3) { throw 'signed known-abusable driver in the normal location was not reported at all' }
              if ($l3 -match '^\[CRITICAL\]') { throw ("validly signed driver in System32\drivers was reported CRITICAL (false positive): $l3") }
              if ($l3 -notmatch '^\[WARNING\]') { throw ("expected WARNING for signed known-abusable driver, got: $l3") }
              Remove-Item -LiteralPath $inPlace -Force -EA SilentlyContinue
              # Same name, same valid signature, but STAGED outside the drivers
              # directory -- that is the real BYOVD pattern and must stay CRITICAL.
              Copy-Item -LiteralPath $srcSigned -Destination $staged -Force
              $md3 = Join-Path $env:TEMP ("dz_drv_ci3_{0}" -f $PID)
              New-Item -ItemType Directory -Path $md3 -Force | Out-Null
              $o4 = (.\tools\driver_audit.ps1 -MarkerDir $md3) -join "`n"
              $l4 = (($o4 -split "`n") | Where-Object { $_ -match 'Public\\gdrv\.sys' } | Select-Object -First 1)
              if (-not $l4) { throw 'staged known-abusable driver was not reported' }
              if ($l4 -notmatch '^\[CRITICAL\]') { throw ("staged known-abusable driver must stay CRITICAL, got: $l4") }
              Remove-Item $md3 -Recurse -Force -EA SilentlyContinue
            } finally {
              Remove-Item -LiteralPath $inPlace -Force -EA SilentlyContinue
              Remove-Item -LiteralPath $staged -Force -EA SilentlyContinue
              Remove-Item $md2 -Recurse -Force -EA SilentlyContinue
            }
          } finally {
            Remove-Item $plant -Force -EA SilentlyContinue
            Remove-Item $md -Recurse -Force -EA SilentlyContinue
          }
          Write-Host "driver_audit.ps1 OK (incl. signed-vendor-driver severity split)"
          exit 0

      - name: report_seal.ps1 (tamper-evidence digest) + persistence timestamps
        shell: powershell
        run: |
          $ErrorActionPreference = 'Stop'
          # ---- Seal ------------------------------------------------------
          # The report is written to the machine under suspicion. Without a
          # digest, whoever compromised that machine can edit the report --
          # delete the finding naming their implant -- and nothing in the file
          # would show it.
          $rp = Join-Path $env:TEMP ("dz_seal_ci_{0}.txt" -f $PID)
          $hp = Join-Path $env:TEMP ("dz_seal_ci_{0}.html" -f $PID)
          Set-Content -LiteralPath $rp -Value @('[CRITICAL] example finding','more text') -Encoding UTF8
          Set-Content -LiteralPath $hp -Value '<html>x</html>' -Encoding UTF8
          try {
            $out = (.\tools\report_seal.ps1 -Report $rp -HtmlReport $hp) -join "`n"
            Write-Host $out
            $seal = "$rp.sha256"
            if (-not (Test-Path $seal)) { throw 'no digest sidecar written' }
            $real = (Get-FileHash -LiteralPath $rp -Algorithm SHA256).Hash.ToUpper()
            if ((Get-Content $seal -Raw) -notmatch [regex]::Escape($real)) { throw 'sidecar digest does not match the file' }
            if ($out -notmatch [regex]::Escape($real)) { throw 'digest was not printed to the console' }
            # The honest limit must be stated, not implied.
            if ((Get-Content $seal -Raw) -notmatch 'does not prove the audit itself') { throw 'seal omits the "this is not attestation" limit' }
            # A one-byte edit must break it -- otherwise the seal proves nothing.
            Add-Content -LiteralPath $rp -Value 'tampered'
            $after = (Get-FileHash -LiteralPath $rp -Algorithm SHA256).Hash.ToUpper()
            if ($after -eq $real) { throw 'digest did not change after the file was edited' }
            if ((Get-Content $seal -Raw) -match [regex]::Escape($after)) { throw 'sidecar somehow matches the tampered file' }
          } finally { Remove-Item $rp, $hp, "$rp.sha256" -Force -EA SilentlyContinue }

          # ---- Persistence timestamps ------------------------------------
          # A finding says WHAT was found; until now it never said WHEN it
          # appeared -- the question that decides whether an implant predates a
          # relationship, a job, or a break-in.
          $runKey = 'HKCU:\Software\Microsoft\Windows\CurrentVersion\Run'
          $probe  = 'dz_ci_when_probe'
          if (-not (Test-Path $runKey)) { New-Item -Path $runKey -Force | Out-Null }
          # Point the autorun at a REAL FILE in a staging path, so the file half
          # of the "when:" line is exercised too and the assertion does not
          # depend solely on the registry P/Invoke succeeding. (PowerShell's
          # registry provider does not expose a key's LastWriteTime, so that
          # half comes from RegQueryInfoKey and degrades to omitted, never to a
          # made-up date.)
          $probeFile = 'C:\Users\Public\dz_ci_when_probe.exe'
          Set-Content -LiteralPath $probeFile -Value 'MZ not a real binary' -Encoding ASCII
          Set-ItemProperty -Path $runKey -Name $probe -Value "$probeFile -w hidden" -Force
          try {
            $pe = (.\tools\persistence_eval.ps1) -join "`n"
            Write-Host (($pe -split "`n" | Where-Object { $_ -match "$probe|when:|note:" }) -join "`n")
            $lines = $pe -split "`r?`n"
            $ix = -1
            for ($i = 0; $i -lt $lines.Count; $i++) { if ($lines[$i] -match [regex]::Escape($probe)) { $ix = $i; break } }
            if ($ix -lt 0) { throw 'planted autorun was not reported at all' }
            $window = ($lines[$ix..([Math]::Min($ix + 4, $lines.Count - 1))]) -join "`n"
            if ($window -notmatch '(?m)^\s+when:.*\d{4}-\d{2}-\d{2} \d{2}:\d{2}:\d{2}') { throw "finding carries no timestamped 'when:' line: $window" }
            # The caveat is what keeps the timestamp from being read as proof.
            if ($pe -notmatch 'timestomping') { throw 'timestamp caveat (per-key / timestomping) is missing' }
            # The continuation line must NOT open with a tag -- block_sev.ps1,
            # report_safety.ps1 and the findings lint all key on the leading
            # tag, so a tagged continuation would inflate every severity count.
            foreach ($l in $lines) {
              if ($l -match '^\s*\[(CRITICAL|WARNING)\].*when:') { throw "a 'when:' line opened with a severity tag: $l" }
            }
            # Both halves must be reachable: the file time always, and the
            # registry time whenever RegQueryInfoKey works on this host.
            if ($window -notmatch 'file written') { throw "the file half of the when: line is missing: $window" }
            if ($window -notmatch 'registry key last modified') { Write-Host '  NOTE: registry key time unavailable on this host -- file half only (degradation is by design)' }
          } finally {
            Remove-ItemProperty -Path $runKey -Name $probe -EA SilentlyContinue
            Remove-Item -LiteralPath $probeFile -Force -EA SilentlyContinue
          }
          Write-Host "report_seal + persistence timestamps OK"
          exit 0

      - name: log_gap_check.ps1 (records missing with no clear event)
        shell: powershell
        run: |
          $ErrorActionPreference = 'Stop'
          # THE ARITHMETIC, PROVEN. Selective record deletion cannot be planted
          # safely on a runner, so the decision lives in a pure function and
          # -SelfTest runs it over fixed inputs. Without this the live run below
          # would only prove the script executes, not that it can tell a rolled
          # log from a gutted one -- and those two cases are the whole point.
          $st = (.\tools\log_gap_check.ps1 -SelfTest) -join "`n"
          Write-Host $st
          if ($LASTEXITCODE -ne 0) { throw 'log_gap_check -SelfTest failed: the gap arithmetic is wrong' }
          if ($st -notmatch 'records removed from the middle') { throw 'self-test no longer covers the middle-removal case' }
          if ($st -notmatch 'rolled over') { throw 'self-test no longer covers normal rollover -- the false-positive guard is unproven' }
          $cases = ([regex]::Matches($st, '(?m)^\[OK\]')).Count
          if ($cases -lt 7) { throw "self-test ran only $cases case(s) -- it is not exercising the decision function" }
          if ($st -notmatch 'was a false positive') { throw 'the rolled-log regression case was removed -- the CI false positive can return' }

          # LIVE RUN. A healthy runner must not raise: normal circular rollover
          # keeps the numbering consistent, so a finding here would be a false
          # positive of exactly the kind this project keeps fixing.
          $md = Join-Path $env:TEMP ("dz_lg_ci_{0}" -f $PID)
          New-Item -ItemType Directory -Path $md -Force | Out-Null
          try {
            $out = (.\tools\log_gap_check.ps1 -MarkerDir $md) -join "`n"
            Write-Host $out
            $seen = ([regex]::Matches($out, "Event log '")).Count
            if ($seen -lt 3) { throw "only $seen log(s) were inspected -- the check verified almost nothing" }
            if ($out -notmatch 'event log\(s\) inspected') { throw 'no inspection tally emitted' }
            $m = Join-Path $md 'dz_loggap.txt'
            if (Test-Path $m) {
              $sev = (Get-Content $m).Trim()
              # A WARNING is acceptable on a runner (the Security log is often
              # capped small, and PowerShell/Operational may be absent); a
              # CRITICAL would mean it thinks records were deleted, which on a
              # fresh CI VM is a false positive worth failing on.
              if ($sev -eq 'CRITICAL') { throw "log_gap_check reported CRITICAL on a clean runner (false positive): $out" }
              Write-Host "  (marker $sev on the runner -- acceptable; CRITICAL would not be)"
            }
            # The honest framing must survive: this reports an accounting
            # inconsistency, not proven deletion.
            $src = Get-Content .\tools\log_gap_check.ps1 -Raw
            if ($src -notmatch 'not proof of deletion') { throw 'the "inconsistency, not proof" framing was removed' }
          } finally { Remove-Item $md -Recurse -Force -EA SilentlyContinue }
          Write-Host "log_gap_check.ps1 OK"
          exit 0

      - name: section_coverage.ps1 (a CLEAN verdict must not cover checks that never ran)
        shell: powershell
        run: |
          $ErrorActionPreference = 'Stop'
          $rp = Join-Path $env:TEMP ("dz_sc_ci_{0}.txt" -f $PID)
          # Section 9 skipped two checks; section 10 skipped none but MENTIONS
          # the word in prose; section 11 skipped one but found issues, so its
          # verdict must be left alone.
          Set-Content -LiteralPath $rp -Encoding UTF8 -Value @(
            '===================================================================='
            ' [9/18] DEFENDER AND AV STATUS'
            '===================================================================='
            '[SKIPPED] Get-MpPreference failed -- path-exclusion check NOT performed.'
            '[OK] something fine'
            '[SKIPPED] Get-MpComputerStatus failed -- core status NOT evaluated.'
            ' [SECTION 9/18 RESULT: CLEAN -- no issues detected]'
            '===================================================================='
            ' [10/18] SMB, RDP, REMOTE ACCESS'
            '===================================================================='
            '[OK] everything ran'
            '[INFO] this line mentions [SKIPPED] in prose but is not one'
            ' [SECTION 10/18 RESULT: CLEAN -- no issues detected]'
            '===================================================================='
            ' [11/18] POWERSHELL'
            '===================================================================='
            '[SKIPPED] provider unavailable'
            ' [SECTION 11/18 RESULT: ISSUES FOUND -- review [WARNING] entries above]'
          )
          try {
            $out = (.\tools\section_coverage.ps1 -Report $rp) -join "`n"
            Write-Host $out
            $m = [regex]::Match($out, 'Calibrated (\d+) section verdict')
            if (-not $m.Success) { throw 'no calibration tally emitted' }
            if ([int]$m.Groups[1].Value -ne 1) { throw "expected exactly 1 calibrated verdict, got $($m.Groups[1].Value)" }
            $txt = Get-Content -LiteralPath $rp -Raw
            # Section 9: calibrated, and CLEAN is still the FIRST token after
            # RESULT: -- detection_selftest's section-sync assertion reads that
            # token, and every other consumer keys on 'ISSUES FOUND'.
            if ($txt -notmatch '\[SECTION 9/18 RESULT: CLEAN -- no issues detected, but 2 checks in this section could NOT run') { throw 'section 9 verdict was not calibrated' }
            # Section 10: prose mention must NOT count.
            if ($txt -notmatch '\[SECTION 10/18 RESULT: CLEAN -- no issues detected\]') { throw 'section 10 was calibrated from a prose mention of [SKIPPED]' }
            # Section 11: ISSUES FOUND is never touched.
            if ($txt -notmatch '\[SECTION 11/18 RESULT: ISSUES FOUND') { throw 'an ISSUES FOUND verdict was rewritten' }
            # No denominator invented: the tool cannot know how many checks ran.
            if ($txt -match 'of \d+ checks') { throw 'a checks-attempted ratio was invented' }
            # Idempotent: running twice must not double-append.
            $out2 = (.\tools\section_coverage.ps1 -Report $rp) -join "`n"
            $m2 = [regex]::Match($out2, 'Calibrated (\d+) section verdict')
            if ([int]$m2.Groups[1].Value -ne 0) { throw 'second run re-calibrated an already-calibrated verdict' }
          } finally { Remove-Item $rp -Force -EA SilentlyContinue }
          Write-Host "section_coverage.ps1 OK"
          exit 0

      - name: edr_presence.ps1 (telemetry inventory; a stopped agent is the finding)
        shell: powershell
        run: |
          $ErrorActionPreference = 'Stop'
          $md = Join-Path $env:TEMP ("dz_edr_ci_{0}" -f $PID)
          # Deliberately NOT pre-created: Write-Marker must create the marker
          # directory itself. A field test lost a real WARNING to exactly this
          # -- the write failed silently into a missing directory.
          try {
            # CLEAN RUNNER: no third-party EDR, no Sysmon. That is the NORM on
            # a consumer machine, so it must be [INFO] and must NOT write a
            # marker -- raising it would fire on nearly every home PC and teach
            # the reader to skip Section 9, the exact false-positive pattern
            # fixed for ADFS, Azure AD Connect and the default screensaver.
            $out = (.\tools\edr_presence.ps1 -MarkerDir $md) -join "`n"
            Write-Host $out
            if (Test-Path (Join-Path $md 'dz_edr.txt')) {
              throw ("edr_presence raised on a clean runner (false positive): " + (Get-Content (Join-Path $md 'dz_edr.txt')))
            }
            if ($out -notmatch 'Sysmon is not installed') { throw 'no Sysmon verdict emitted' }
            if ($out -notmatch 'inspected') { throw 'no inspection tally emitted' }
            # Guard the guard: a run that enumerated nothing must not look clean.
            $m = [regex]::Match($out, '(\d+) security product/agent\(s\) inspected')
            if (-not $m.Success) { throw 'inspection tally not parseable' }
            # The honest framing must survive: absence is context, not a fault.
            if ($out -notmatch 'Not a fault') { throw 'the "absence is not a fault" framing was removed' }

            # THE ONE BRANCH THAT PRODUCES A FINDING must be proven, not
            # assumed: plant a STOPPED service using an EDR name the tool
            # knows, and require a WARNING plus a marker.
            & sc.exe create SentinelAgent binPath= 'C:\Windows\System32\cmd.exe /c rem dz_ci_probe' start= demand | Out-Null
            $md2 = Join-Path $env:TEMP ("dz_edr_ci2_{0}" -f $PID)
            try {
              $out2 = (.\tools\edr_presence.ps1 -MarkerDir $md2) -join "`n"
              Write-Host (($out2 -split "`n" | Where-Object { $_ -match 'SentinelOne|NOT RUNNING' }) -join "`n")
              if ($out2 -notmatch 'INSTALLED BUT NOT RUNNING') { throw 'a stopped EDR service was not reported' }
              $m2 = Join-Path $md2 'dz_edr.txt'
              if (-not (Test-Path $m2)) { throw 'stopped EDR service wrote no marker -- the finding never reaches the ledger' }
              if ((Get-Content $m2).Trim() -ne 'WARNING') { throw ("expected WARNING, got " + (Get-Content $m2)) }
              # The summary must not contradict the finding above it: a stopped
              # agent was DETECTED, so the report may not also claim none was.
              if ($out2 -match 'No third-party EDR agent was detected') { throw 'summary claims no third-party EDR while warning about a stopped one' }
            } finally {
              & sc.exe delete SentinelAgent 2>&1 | Out-Null
              Remove-Item $md2 -Recurse -Force -EA SilentlyContinue
            }

            # A machine never onboarded to Defender for Endpoint ships the
            # 'Sense' service stopped. That is the NORM on consumer PCs and
            # must be [INFO] with no marker -- a field test on a real home PC
            # hit this exact false positive. Only plant if the runner has no
            # real Sense service of its own.
            $senseExists = $true
            try { Get-Service -Name Sense -EA Stop | Out-Null } catch { $senseExists = $false }
            if (-not $senseExists) {
              & sc.exe create Sense binPath= 'C:\Windows\System32\cmd.exe /c rem dz_ci_probe' start= demand | Out-Null
              $md3 = Join-Path $env:TEMP ("dz_edr_ci3_{0}" -f $PID)
              try {
                $out3 = (.\tools\edr_presence.ps1 -MarkerDir $md3) -join "`n"
                if ($out3 -match "INSTALLED BUT NOT RUNNING \(service 'Sense'") { throw 'a never-onboarded Sense service was flagged as a stopped EDR (false positive)' }
                if ($out3 -notmatch 'never been onboarded') { throw 'no onboarding-state explanation emitted for the inert Sense service' }
                if (Test-Path (Join-Path $md3 'dz_edr.txt')) { throw 'inert Sense service raised a marker (false positive)' }
              } finally {
                & sc.exe delete Sense 2>&1 | Out-Null
                Remove-Item $md3 -Recurse -Force -EA SilentlyContinue
              }
            } else {
              Write-Host 'Sense exists on this runner; skipping the never-onboarded plant.'
            }
          } finally { Remove-Item $md -Recurse -Force -EA SilentlyContinue }
          Write-Host "edr_presence.ps1 OK"
          exit 0

      - name: report_safety.ps1 (Tier 0 preamble + coverage framing)
        shell: powershell
        run: |
          $ErrorActionPreference = 'Stop'
          $pre = (.\tools\report_safety.ps1 -Mode Preamble) -join "`n"
          foreach ($needle in @('READ THIS FIRST','CLEAN" result does NOT prove','accessnow.org/help','stopstalkerware.org','PRESERVE EVIDENCE')) {
            if ($pre -notmatch [regex]::Escape($needle)) { throw ("preamble missing: {0}" -f $needle) }
          }
          # Coverage mode over a synthetic report with a SKIPPED line and an
          # auditing-off line must surface both in its confidence summary.
          $rp = Join-Path $env:TEMP ("dz_rs_ci_{0}.txt" -f $PID)
          Set-Content -LiteralPath $rp -Value @('[CRITICAL] x','[SKIPPED] y','Process Creation (4688) auditing is OFF [(not set)]') -Encoding ASCII
          try {
            $cov = (.\tools\report_safety.ps1 -Mode Coverage -Report $rp) -join "`n"
            Write-Host $cov
            if ($cov -notmatch 'COVERAGE & CONFIDENCE') { throw 'coverage header missing' }
            if ($cov -notmatch 'Checks SKIPPED    : 1') { throw 'skipped count not surfaced' }
            if ($cov -notmatch 'Audit visibility  : REDUCED') { throw 'reduced audit visibility not surfaced' }
          } finally { Remove-Item $rp -Force -EA SilentlyContinue }

          # A MISSING HELPER IS A CHECK THAT DID NOT RUN. doze_sec.bat reports
          # those as "[INFO] tools\X.ps1 not found -- ... skipped", which the
          # [SKIPPED] test never matched -- so deleting stalkerware_check.ps1,
          # the one check that models an abusive partner, left this block
          # asserting "every attempted check produced a result".
          # And the counters were unanchored, so the TOP FINDINGS block
          # top_findings.ps1 prepends (which QUOTES each finding) counted every
          # finding a second time.
          $rp2 = Join-Path $env:TEMP ("dz_rs_ci2_{0}.txt" -f $PID)
          Set-Content -LiteralPath $rp2 -Encoding ASCII -Value @(
            '  TOP FINDINGS'
            '  1. [CRITICAL] quoted copy of the finding below'
            '  2. [WARNING] quoted copy of a warning'
            '[CRITICAL] the one real critical'
            '[INFO] tools\stalkerware_check.ps1 not found -- covert-monitoring check skipped.'
          )
          try {
            $cov2 = (.\tools\report_safety.ps1 -Mode Coverage -Report $rp2) -join "`n"
            Write-Host $cov2
            if ($cov2 -notmatch 'Checks SKIPPED    : 1') { throw 'a missing helper was not counted as a skipped check' }
            if ($cov2 -notmatch '1 critical, 0 warning') { throw ('quoted TOP FINDINGS lines inflated the counts: ' + $cov2) }
            if ($cov2 -match 'every attempted check produced a result') { throw 'coverage block certified full coverage while a helper was missing' }
          } finally { Remove-Item $rp2 -Force -EA SilentlyContinue }
          Write-Host "report_safety.ps1 OK (incl. missing-helper skips and anchored counting)"
          exit 0

      - name: baseline_diff.ps1 (save/diff round-trip; new item must be detected)
        shell: powershell
        run: |
          $ErrorActionPreference = 'Stop'
          $md = Join-Path $env:TEMP ("dz_bl_ci_{0}" -f $PID)
          New-Item -ItemType Directory -Path $md -Force | Out-Null
          $snap = Join-Path $md 'baseline.snapshot'
          $runKey = 'HKCU:\Software\Microsoft\Windows\CurrentVersion\Run'
          try {
            # No baseline yet -> must guide, never fail and never fake a clean pass.
            $none = (.\tools\baseline_diff.ps1 -Mode Diff -Path $snap -MarkerDir $md) -join "`n"
            if ($none -notmatch 'No baseline found') { throw 'missing-baseline path did not report guidance' }
            if (Test-Path (Join-Path $md 'dz_baseline.txt')) { throw 'missing baseline wrote a marker' }
            # Save, then diff with no changes -> must be quiet.
            $save = (.\tools\baseline_diff.ps1 -Mode Save -Path $snap -MarkerDir $md) -join "`n"
            Write-Host $save
            if (-not (Test-Path $snap)) { throw 'baseline snapshot not written' }
            if ($save -notmatch 'not proof the current state is clean') { throw 'save output missing the honest-limitation note' }
            # Snapshot must be deterministic: re-saving yields identical records
            # (header carries the timestamp, so compare only record lines).
            $recs1 = @(Get-Content $snap | Where-Object { $_ -notmatch '^#' })
            if ($recs1.Count -lt 10) { throw ("snapshot suspiciously small: {0} records" -f $recs1.Count) }
            # Plant a NEW autorun and confirm the diff reports it as NEW+WARNING.
            if (-not (Test-Path $runKey)) { New-Item -Path $runKey -Force | Out-Null }
            Set-ItemProperty -Path $runKey -Name 'dz_ci_baseline_probe' -Value 'C:\Windows\Temp\dz_ci_probe.exe' -Force
            $diff = (.\tools\baseline_diff.ps1 -Mode Diff -Path $snap -MarkerDir $md) -join "`n"
            Write-Host ($diff -split "`n" | Where-Object { $_ -match 'dz_ci_baseline_probe|summary' })
            if ($diff -notmatch 'NEW autorun/persistence value since baseline') { throw 'planted autorun not reported as NEW' }
            if ($diff -notmatch 'dz_ci_baseline_probe') { throw 'planted autorun name missing from diff' }
            $m = Join-Path $md 'dz_baseline.txt'
            if (-not (Test-Path $m)) { throw 'baseline diff wrote no marker for a new persistence value' }
            if ((Get-Content $m).Trim() -ne 'WARNING') { throw ("expected WARNING marker, got {0}" -f (Get-Content $m)) }

            # A NEW SERVICE HOSTED BY A SIGNED LOLBIN MUST NOT BE DOWNGRADED.
            # The signature gate keyed on the host binary only, so
            # `rundll32.exe <staging>\x.dll,Run` resolved to a Microsoft-signed
            # image, became INFO, was excluded from the WARNING set, raised no
            # severity and wrote no marker -- and the INFO line omitted the
            # command line, so the malicious argument never even appeared. That
            # is LOLBin persistence rendered as a Windows-update notice in the
            # check that exists to catch implants matching no signature.
            & sc.exe create dz_ci_lolbin binPath= 'C:\Windows\System32\rundll32.exe C:\ProgramData\dz_ci_stage\x.dll,Run' start= demand | Out-Null
            $diff2 = (.\tools\baseline_diff.ps1 -Mode Diff -Path $snap -MarkerDir $md) -join "`n"
            Write-Host ($diff2 -split "`n" | Where-Object { $_ -match 'dz_ci_lolbin' })
            if ($diff2 -notmatch 'dz_ci_lolbin') { throw 'new LOLBin-hosted service missing from the diff entirely' }
            $line = (($diff2 -split "`n") | Where-Object { $_ -match 'dz_ci_lolbin' } | Select-Object -First 1)
            if ($line -notmatch '^\[WARNING\]') { throw ("LOLBin-hosted service was not raised as WARNING: $line") }
            if ($line -notmatch 'rundll32') { throw ("the command line was omitted from the finding: $line") }
          } finally {
            & sc.exe delete dz_ci_lolbin 2>&1 | Out-Null
            Remove-ItemProperty -Path $runKey -Name 'dz_ci_baseline_probe' -EA SilentlyContinue
            Remove-Item $md -Recurse -Force -EA SilentlyContinue
          }
          Write-Host "baseline_diff.ps1 OK (incl. signed-LOLBin service not downgraded)"
          exit 0

      - name: cross_api_check.ps1 (rootkit discrepancy; race-tolerance + Tarrask logic)
        shell: powershell
        run: |
          $ErrorActionPreference = 'Stop'
          $md = Join-Path $env:TEMP ("dz_ca_ci_{0}" -f $PID)
          New-Item -ItemType Directory -Path $md -Force | Out-Null
          $tc = "HKCU:\Software\dz_ci_taskcache_$PID"
          try {
            # Clean runner: processes/services churn constantly, so this is the
            # real false-positive gate -- if race re-verification is broken, a
            # healthy machine reports a phantom hidden process every run.
            $out = (.\tools\cross_api_check.ps1 -MarkerDir $md) -join "`n"
            Write-Host $out
            if ($out -match '\[CRITICAL\]') { throw "cross_api_check reported CRITICAL on a clean runner (race handling or enumeration bug)" }
            # A WARNING marker is allowed here: reading TaskCache SD values can
            # require SYSTEM, and reporting that blindness as [SKIPPED]+WARNING is
            # the honest outcome. A CRITICAL marker is not -- that is a false
            # accusation, which is what this gate exists to prevent.
            $cm = Join-Path $md 'dz_crossapi.txt'
            if (Test-Path $cm) {
              $cv = (Get-Content $cm).Trim()
              if ($cv -eq 'CRITICAL') { throw "crossapi wrote a CRITICAL marker on a clean runner" }
              Write-Host ("  note: clean-runner marker is {0} (acceptable -- a skipped check, not an accusation)" -f $cv)
            }
            if ($out -notmatch 'Process lists agree') { throw 'process cross-check did not run' }
            if ($out -notmatch 'Service views agree') { throw 'service cross-check did not run' }
            # Tarrask logic against a SYNTHETIC hive -- never touches the real
            # TaskCache, so the live Task Scheduler is unaffected.
            $guid = '{dead0000-0000-0000-0000-00000000cafe}'
            New-Item -Path "$tc\Tree\dz_ci_hidden" -Force | Out-Null
            Set-ItemProperty -Path "$tc\Tree\dz_ci_hidden" -Name 'Id' -Value $guid -Force
            New-Item -Path "$tc\Tasks\$guid" -Force | Out-Null
            # (a) Tasks entry WITHOUT an SD value AND absent from the real Task
            # Scheduler (it only exists in this synthetic hive) = both signals
            # Tarrask requires, so it must be reported.
            $md2 = Join-Path $md 'tarrask'
            New-Item -ItemType Directory -Path $md2 -Force | Out-Null
            $t1 = (.\tools\cross_api_check.ps1 -MarkerDir $md2 -TreeRoot "$tc\Tree" -TasksRoot "$tc\Tasks") -join "`n"
            Write-Host ($t1 -split "`n" | Where-Object { $_ -match 'Tarrask|security descriptor|dz_ci_hidden' })
            if ($t1 -notmatch 'NO security descriptor') { throw 'missing-SD task was not reported as Tarrask-style hidden' }
            if ((Get-Content (Join-Path $md2 'dz_crossapi.txt')).Trim() -ne 'CRITICAL') { throw 'Tarrask finding did not write a CRITICAL marker' }
            # (b) Same task WITH an SD value must be clean -- proves the check
            # keys on the missing descriptor, not merely on the task existing.
            Set-ItemProperty -Path "$tc\Tasks\$guid" -Name 'SD' -Value ([byte[]](1,2,3,4)) -Type Binary -Force
            $md3 = Join-Path $md 'withsd'
            New-Item -ItemType Directory -Path $md3 -Force | Out-Null
            $t2 = (.\tools\cross_api_check.ps1 -MarkerDir $md3 -TreeRoot "$tc\Tree" -TasksRoot "$tc\Tasks") -join "`n"
            if ($t2 -match 'NO security descriptor') { throw 'task WITH an SD was still reported as hidden (false positive)' }
            # (c) SCHEDULER UNAVAILABLE = the check CANNOT run. The rule is
            # "missing SD AND absent from the scheduler", so without the
            # scheduler no hit is reachable -- yet the tool used to print
            # "[INFO] ... ran without its second signal" and then "[OK] No
            # hidden scheduled tasks ... all carry a security descriptor". Two
            # false statements on exactly the host an implant that just planted
            # a hidden task would produce. It must now decline to claim a clean
            # result and raise the blind spot instead.
            $md4 = Join-Path $md 'nolive'
            New-Item -ItemType Directory -Path $md4 -Force | Out-Null
            $t3 = (.\tools\cross_api_check.ps1 -MarkerDir $md4 -TreeRoot "$tc\Tree" -TasksRoot "$tc\Tasks" -SkipLiveTaskCompare) -join "`n"
            Write-Host ($t3 -split "`n" | Where-Object { $_ -match 'hidden|scheduler|Scheduler' })
            if ($t3 -match '\[OK\] No hidden scheduled tasks') { throw 'claimed a clean hidden-task result while the check could not run' }
            # The proportion guard is the OTHER path that empties $noSd and
            # declares the check not performed; it used to print the SKIPPED
            # line and then, one branch later, "all carry a security
            # descriptor" -- contradicting itself in adjacent lines.
            $src = Get-Content .\tools\cross_api_check.ps1 -Raw
            if ($src -notmatch '\$sdSuppressed') { throw 'the proportion-guard suppression flag is gone -- the self-contradicting [OK] can return' }
            if ($src -notmatch '\$noSd\.Count -eq 0 -and \$liveOk -and -not \$sdSuppressed') { throw 'the hidden-task [OK] is no longer guarded on both degradation paths' }
            if ($t3 -notmatch '\[SKIPPED\][^\r\n]*hidden-task') { throw 'degraded hidden-task path did not report itself as SKIPPED' }
            if (-not (Test-Path (Join-Path $md4 'dz_crossapi.txt'))) { throw 'degraded hidden-task path raised no finding at all' }
          } finally {
            Remove-Item $tc -Recurse -Force -EA SilentlyContinue
            Remove-Item $md -Recurse -Force -EA SilentlyContinue
          }
          Write-Host "cross_api_check.ps1 OK (incl. honest degradation when the scheduler is unavailable)"
          exit 0

      - name: module_inspect.ps1 (injection detection; staged DLL must be caught)
        shell: powershell
        run: |
          $ErrorActionPreference = 'Stop'
          $md = Join-Path $env:TEMP ("dz_mi_ci_{0}" -f $PID)
          New-Item -ItemType Directory -Path $md -Force | Out-Null
          $staged = "C:\Users\Public\dz_ci_probe_mod.dll"
          $sig = '[DllImport("kernel32", CharSet=CharSet.Unicode)] public static extern IntPtr LoadLibraryW(string p); [DllImport("kernel32")] public static extern bool FreeLibrary(IntPtr h);'
          Add-Type -Name DzK -Namespace DzCi -MemberDefinition $sig | Out-Null
          $h = [IntPtr]::Zero
          try {
            # Clean runner first: a healthy box loads everything from signed
            # system locations, so any CRITICAL here is a false positive.
            $out = (.\tools\module_inspect.ps1 -MarkerDir $md) -join "`n"
            Write-Host ($out -split "`n" | Select-Object -First 15)
            if ($out -match '\[CRITICAL\]') { throw "module_inspect reported CRITICAL on a clean runner (false positive)" }
            # Load a REAL signed Microsoft DLL from a staging path into THIS
            # process, then inspect. Severity must come from the staging path,
            # not the signature -- that is the injection shape being modelled.
            Copy-Item "$env:SystemRoot\System32\version.dll" $staged -Force
            $h = [DzCi.DzK]::LoadLibraryW($staged)
            if ($h -eq [IntPtr]::Zero) { throw 'could not load the staged probe DLL' }
            $md2 = Join-Path $md 'staged'
            New-Item -ItemType Directory -Path $md2 -Force | Out-Null
            $out2 = (.\tools\module_inspect.ps1 -MarkerDir $md2) -join "`n"
            Write-Host ($out2 -split "`n" | Where-Object { $_ -match 'dz_ci_probe_mod' })
            if ($out2 -notmatch 'dz_ci_probe_mod\.dll') { throw 'staged module not reported' }
            if ($out2 -notmatch 'staging path') { throw 'staged module not attributed to the staging path' }
            $m = Join-Path $md2 'dz_module.txt'
            if (-not (Test-Path $m)) { throw 'staged module wrote no marker' }
            if ((Get-Content $m).Trim() -ne 'CRITICAL') { throw ("expected CRITICAL, got {0}" -f (Get-Content $m)) }
          } finally {
            if ($h -ne [IntPtr]::Zero) { [void][DzCi.DzK]::FreeLibrary($h) }
            Remove-Item $staged -Force -EA SilentlyContinue
            Remove-Item $md -Recurse -Force -EA SilentlyContinue
          }
          Write-Host "module_inspect.ps1 OK"
          exit 0

      - name: module_inspect / emulation_coverage regression guards (batch 4)
        shell: powershell
        run: |
          $ErrorActionPreference = 'Stop'
          # An UNBACKED module -- mapped into a process with no file on disk --
          # is the classic in-memory injection signal. It used to be folded into
          # the $unsignedOther tally and then explained away as "common for
          # legitimate third-party software", so the one thing this check exists
          # to surface was counted as benign.
          $mi = Get-Content .\tools\module_inspect.ps1 -Raw
          if ($mi -notmatch "elseif \(\`$v\.Why -match 'not found on disk'\)") { throw 'unbacked modules are no longer separated from the benign unsigned tally' }
          if ($mi -notmatch 'reflective or unbacked load') { throw 'the unbacked-load finding text is gone' }
          # Staged paths must be inspected BEFORE the cap can discard them.
          if ($mi -notmatch '\$ordered = ') { throw 'staged-path-first ordering was removed; the cap can again drop exactly what the check looks for' }

          # An Invert case plants a BENIGN state and asserts the detection does
          # NOT fire. Counting its Attack tag let a CORE technique's "must stay
          # emulated" invariant be satisfied by a test proving the detection
          # stays silent. Prove the parser now skips Invert cases AND still
          # finds every CORE plant (a too-strict parser would fail -Strict on a
          # corpus that is actually correct).
          $out = (.\tools\emulation_coverage.ps1 -SourceDir . -Strict) -join "`n"
          Write-Host $out
          if ($LASTEXITCODE -ne 0) { throw 'emulation_coverage -Strict failed after the Invert-case fix -- a CORE plant was lost' }
          $ec = Get-Content .\tools\emulation_coverage.ps1 -Raw
          if ($ec -notmatch 'Invert[^\r\n]*continue') { throw 'the Invert-case exclusion is gone' }
          $m = [regex]::Match($out, 'Emulated by a plant\s+:\s+(\d+)')
          if (-not $m.Success -or [int]$m.Groups[1].Value -lt 20) { throw "per-case parser found only $($m.Groups[1].Value) emulated techniques -- it is dropping cases" }
          Write-Host "batch 4 regression guards OK"
          exit 0

      - name: stalkerware_check.ps1 (covert monitoring; consent inventory must not accuse)
        shell: powershell
        run: |
          $ErrorActionPreference = 'Stop'
          $md = Join-Path $env:TEMP ("dz_sw_ci_{0}" -f $PID)
          New-Item -ItemType Directory -Path $md -Force | Out-Null
          $ul = 'HKLM:\SOFTWARE\Microsoft\Windows NT\CurrentVersion\Winlogon\SpecialAccounts\UserList'
          $madeUl = $false
          try {
            # Clean runner: camera/mic permissions are ordinary, so the consent
            # inventory must stay INFORMATIONAL. Any CRITICAL here means the
            # module is accusing a healthy machine -- the failure mode that
            # matters most for the people this check is written for.
            $out = (.\tools\stalkerware_check.ps1 -MarkerDir $md) -join "`n"
            Write-Host $out
            if ($out -match '\[CRITICAL\]') { throw "stalkerware_check reported CRITICAL on a clean runner" }
            if ($out -notmatch 'sign-in screen') { throw 'hidden-account check did not run' }
            if ($out -notmatch 'shadowing') { throw 'RDP shadowing check did not run' }
            if ($out -notmatch 'monitoring/spouseware|monitoring another person') { throw 'monitoring-product check did not run' }
            # Plant a hidden account entry: must be reported, and must be worded
            # as something to review rather than as an accusation.
            if (-not (Test-Path $ul)) { New-Item -Path $ul -Force | Out-Null; $madeUl = $true }
            Set-ItemProperty -Path $ul -Name 'dz_ci_probe_user' -Value 0 -Type DWord -Force
            $md2 = Join-Path $md 'hidden'
            New-Item -ItemType Directory -Path $md2 -Force | Out-Null
            $out2 = (.\tools\stalkerware_check.ps1 -MarkerDir $md2) -join "`n"
            Write-Host ($out2 -split "`n" | Where-Object { $_ -match 'dz_ci_probe_user|HIDDEN' })
            if ($out2 -notmatch 'dz_ci_probe_user') { throw 'planted hidden account not reported' }
            $m = Join-Path $md2 'dz_stalkerware.txt'
            if (-not (Test-Path $m)) { throw 'planted hidden account wrote no marker' }
            if ((Get-Content $m).Trim() -ne 'WARNING') { throw ("expected WARNING, got {0}" -f (Get-Content $m)) }
            # A hidden account is suspicious, not proof -- legitimate software
            # hides service accounts. The wording must not condemn.
            if ($out2 -notmatch 'legitimate software') { throw 'hidden-account finding lacks the not-necessarily-malicious framing' }

            # A JUNK VALUE MUST NOT KILL THE SCAN. `[int]$p.Value` threw on any
            # non-numeric UserList value and the try/catch wrapped the WHOLE
            # loop, so one REG_SZ entry aborted the scan and the tool then
            # printed "No accounts are hidden from the sign-in screen" on a
            # machine that had one. This is the check that models an abusive
            # partner; a silent false clean here is the worst outcome in the
            # tool. Plant junk ALONGSIDE the real hidden account.
            Set-ItemProperty -Path $ul -Name 'dz_ci_junk_value' -Value 'n/a' -Type String -Force
            $md3 = Join-Path $md 'junk'
            New-Item -ItemType Directory -Path $md3 -Force | Out-Null
            $out3 = (.\tools\stalkerware_check.ps1 -MarkerDir $md3) -join "`n"
            Write-Host ($out3 -split "`n" | Where-Object { $_ -match 'dz_ci_probe_user|hidden from the sign-in' })
            if ($out3 -notmatch 'dz_ci_probe_user') { throw 'a non-numeric UserList value aborted the hidden-account scan -- the real hidden account was not reported' }
            if ($out3 -match 'No accounts are hidden from the sign-in screen') { throw 'reported a clean hidden-account verdict while an account was hidden' }
            Remove-ItemProperty -Path $ul -Name 'dz_ci_junk_value' -EA SilentlyContinue
          } finally {
            Remove-ItemProperty -Path $ul -Name 'dz_ci_junk_value' -EA SilentlyContinue
            Remove-ItemProperty -Path $ul -Name 'dz_ci_probe_user' -EA SilentlyContinue
            if ($madeUl) { Remove-Item $ul -Force -EA SilentlyContinue }
            Remove-Item $md -Recurse -Force -EA SilentlyContinue
          }
          Write-Host "stalkerware_check.ps1 OK (incl. junk-value resilience)"
          exit 0

      - name: attack_matrix.ps1 (coverage matrix derived from code; completeness enforced)
        shell: powershell
        run: |
          $ErrorActionPreference = 'Stop'
          # -Strict makes the tool exit non-zero if any technique referenced in
          # the audit is missing a ttp_manifest mapping. This is the guarantee
          # that the coverage matrix cannot silently drift from what the code
          # actually detects: add a detection with a new technique id and forget
          # to describe it, and THIS job fails until the mapping is added.
          $out = (.\tools\attack_matrix.ps1 -SourceDir . -Strict) -join "`n"
          Write-Host $out
          if ($LASTEXITCODE -ne 0) { throw "attack_matrix -Strict found technique ids referenced by the audit but not mapped in ttp_manifest.txt (see WARNING lines above)" }
          if ($out -notmatch 'ATT&CK COVERAGE MATRIX') { throw 'matrix header missing' }
          if ($out -notmatch 'coverage matrix is complete') { throw 'completeness line missing -- unexpected unmapped techniques' }
          # Sanity: the audit's heaviest tactic must show real coverage, or the
          # source scan silently found nothing (e.g. a path/regex regression).
          if ($out -notmatch '(?m)^Persistence\s+\((\d+)\)') { throw 'Persistence tactic group missing from the matrix' }
          # Prove the gate is not vacuous. Point it at a directory with no audit
          # sources: the completeness check ("every referenced technique is
          # mapped") is trivially satisfied by an empty reference set, so a
          # scan that finds nothing must FAIL under -Strict rather than report
          # a complete matrix. A gate that passes when it checked nothing is
          # not a gate.
          $empty = Join-Path $env:TEMP ("dz_empty_src_{0}" -f $PID)
          New-Item -ItemType Directory -Path $empty -Force | Out-Null
          Copy-Item ThreatLists\ttp_manifest.txt (Join-Path $empty 'ttp_manifest.txt') -Force
          $vac = (.\tools\attack_matrix.ps1 -SourceDir $empty -Manifest (Join-Path $empty 'ttp_manifest.txt') -Strict) -join "`n"
          if ($LASTEXITCODE -eq 0) { Write-Host $vac; throw 'attack_matrix -Strict PASSED on a source tree with no techniques -- the gate can pass vacuously' }
          Remove-Item $empty -Recurse -Force -EA SilentlyContinue
          Write-Host "attack_matrix.ps1 OK (incl. empty-scan guard)"
          exit 0

      - name: block_sev.ps1 (severity read-back that :dz_ps_scan depends on)
        shell: powershell
        run: |
          $ErrorActionPreference = 'Stop'
          # :dz_ps_scan decides whether a block raises a finding purely from
          # what this script returns. If it under-reads, a detection goes
          # silent again -- exactly the failure the mechanism exists to fix --
          # so every parsing rule is asserted here on real 5.1.
          $f = Join-Path $env:TEMP ("dz_blocksev_ci_{0}.txt" -f $PID)
          function Sev($lines) { $lines | Set-Content -LiteralPath $f -Encoding UTF8; (.\tools\block_sev.ps1 -Path $f).Trim() }

          if ((Sev @('[OK] nothing to see', '[INFO] context', '[SKIPPED] not run')) -ne 'OK') { throw 'clean block did not read OK' }
          if ((Sev @('[OK] fine', '[WARNING] Non-default WMI EventFilters found:')) -ne 'WARNING') { throw 'WARNING not detected' }
          if ((Sev @('[WARNING] a', '[CRITICAL] Known malware staging files found:')) -ne 'CRITICAL') { throw 'CRITICAL must win over WARNING' }
          if ((Sev @('  [CRITICAL] indented finding')) -ne 'CRITICAL') { throw 'indented finding missed' }
          if ((Sev @('[WARNING][T1574.001] Unsigned DLLs in system paths:')) -ne 'WARNING') { throw 'tag+technique form missed' }
          if ((Sev @('Word 16.0: [WARNING] blockcontentexecutionfrominternet=0')) -ne 'WARNING') { throw 'mid-line form missed' }
          # Prose must never inflate a block: these are the shapes that would
          # turn a clean machine into a false positive.
          if ((Sev @('[OK] No AMSI bypass patterns in recent PowerShell logs')) -ne 'OK') { throw '[OK] line inflated severity' }
          if ((Sev @('[INFO] Review [WARNING] and [CRITICAL] entries above')) -ne 'OK') { throw '[INFO] prose inflated severity' }
          if ((Sev @('[SKIPPED] WMI subscription query failed -- check NOT performed.')) -ne 'OK') { throw '[SKIPPED] inflated severity' }
          # An unreadable input must not invent a finding.
          if (((.\tools\block_sev.ps1 -Path (Join-Path $env:TEMP 'dz_no_such_file.txt')).Trim()) -ne 'OK') { throw 'missing file did not read OK' }
          Remove-Item $f -Force -EA SilentlyContinue
          Write-Host "block_sev.ps1 OK (10 parsing cases)"
          exit 0

      - name: lint_unraised_findings.ps1 (no check prints a severity it never raises)
        shell: powershell
        run: |
          $ErrorActionPreference = 'Stop'
          # Also run under Windows PowerShell 5.1, not just the Linux lint job:
          # this is the gate that keeps printed-but-unraised findings out, and
          # it must work on the same runtime the project targets.
          $out = (.\tools\lint_unraised_findings.ps1 -Root .) -join "`n"
          Write-Host $out
          if ($LASTEXITCODE -ne 0) { throw 'lint_unraised_findings found checks that print a severity without raising it' }
          if ($out -notmatch 'routed through :dz_ps_scan') { throw 'lint did not report block routing -- staged-block scan regressed' }
          # Guard the guard: if the block scanner ever stops finding blocks, the
          # lint would pass vacuously and the whole class could return.
          $m = [regex]::Match($out, 'Scanned (\d+) staged PowerShell block')
          if (-not $m.Success -or [int]$m.Groups[1].Value -lt 100) { throw "lint scanned only $($m.Groups[1].Value) blocks -- the parser regressed, so a clean result means nothing" }
          Write-Host "lint_unraised_findings.ps1 OK"
          exit 0

      - name: lint_docs_drift.ps1 (documented claims match the code)
        shell: powershell
        run: |
          $ErrorActionPreference = 'Stop'
          $out = (.\tools\lint_docs_drift.ps1 -Root .) -join "`n"
          Write-Host $out
          if ($LASTEXITCODE -ne 0) { throw 'lint_docs_drift found documentation that does not match the code' }
          # Guard the guard: if the parsers stop finding switches or techniques,
          # every comparison becomes trivially true and the lint passes having
          # verified nothing.
          $m = [regex]::Match($out, 'Checked (\d+) documented claim')
          if (-not $m.Success -or [int]$m.Groups[1].Value -lt 10) { throw "docs lint checked only $($m.Groups[1].Value) claims -- a parser regressed, so a clean result means nothing" }
          Write-Host "lint_docs_drift.ps1 OK"
          exit 0

      - name: emulation_coverage.ps1 (adversary-emulation corpus; -Strict guards drift)
        shell: powershell
        run: |
          $ErrorActionPreference = 'Stop'
          # -Strict fails if a CORE detection lost its plant, or if any Attack
          # tag / corpus line names a technique the audit no longer detects.
          # The backlog (detections not yet emulated) is allowed to be non-empty
          # and is printed, not gated -- pretending every technique is testable
          # would be dishonest; silent drift is what this prevents.
          $out = (.\tools\emulation_coverage.ps1 -SourceDir . -Strict) -join "`n"
          Write-Host $out
          if ($LASTEXITCODE -ne 0) { throw "emulation_coverage -Strict found corpus drift (a core detection lost its plant, or a stale tag) -- see WARNING lines above" }
          if ($out -notmatch 'ADVERSARY-EMULATION COVERAGE') { throw 'coverage header missing' }
          if ($out -notmatch 'Emulation corpus is consistent') { throw 'consistency line missing -- unexpected corpus drift' }
          if ($out -notmatch '(?m)^Emulation coverage of the testable surface: \d+%') { throw 'coverage percentage missing -- source scan or harness parse regressed' }
          # Prove the CORE invariant is not vacuous. Every core check is a loop
          # over the parsed CORE set, so a corpus that yields zero CORE entries
          # makes the loop iterate nothing and the tool report "consistent".
          # Feed it a corpus with no CORE lines: -Strict must fail.
          $noCore = Join-Path $env:TEMP ("dz_nocore_{0}.txt" -f $PID)
          (Get-Content tests\emulation_corpus.txt) | Where-Object { $_ -notmatch '^CORE\|' } | Set-Content -LiteralPath $noCore -Encoding UTF8
          $vac = (.\tools\emulation_coverage.ps1 -SourceDir . -Corpus $noCore -Strict) -join "`n"
          if ($LASTEXITCODE -eq 0) { Write-Host $vac; throw 'emulation_coverage -Strict PASSED on a corpus with no CORE entries -- the core-detection invariant can go unchecked' }
          Remove-Item $noCore -Force -EA SilentlyContinue
          Write-Host "emulation_coverage.ps1 OK (incl. empty-CORE guard)"
          exit 0

      - name: boot_chain_check.ps1 (boot-chain config audit; must not false-CRITICAL a clean VM)
        shell: powershell
        run: |
          $ErrorActionPreference = 'Stop'
          $md = Join-Path $env:TEMP ("dz_bc_ci_{0}" -f $PID)
          New-Item -ItemType Directory -Path $md -Force | Out-Null
          try {
            # The runner is a VM: bcdedit works (nointegritychecks is No), while
            # the Secure Boot UEFI vars typically do not exist -> those sub-checks
            # must degrade to [SKIPPED], NOT crash or false-CRITICAL. A CRITICAL
            # here would mean the runner really has kernel integrity disabled OR
            # the bcdedit parse is wrong.
            $out = (.\tools\boot_chain_check.ps1 -MarkerDir $md) -join "`n"
            Write-Host $out
            if ($out -notmatch 'Boot-chain configuration audit') { throw 'boot-chain header missing' }
            if ($out -match '\[CRITICAL\]') { throw "boot_chain_check reported CRITICAL on a clean VM runner (bcdedit parse or gate bug)" }
            if (Test-Path (Join-Path $md 'dz_bootchain.txt')) {
              $mv = (Get-Content (Join-Path $md 'dz_bootchain.txt')).Trim()
              if ($mv -eq 'CRITICAL') { throw "boot_chain_check wrote a CRITICAL marker on a clean VM runner" }
              Write-Host ("  note: clean-runner marker is {0} (acceptable -- e.g. no-bcdedit-output SKIPPED)" -f $mv)
            }
            # The nointegritychecks OK line must appear when bcdedit produced output,
            # proving the integrity-flag parse actually ran on this machine.
            if ($out -match 'nointegritychecks not set') { Write-Host "  integrity-flag parse ran and reported enforced" }
            elseif ($out -match 'bcdedit produced no output') { Write-Host "  bcdedit unavailable on this runner -- integrity parse skipped (honest)" }
            else { throw 'neither the integrity-OK line nor the bcdedit-skip line appeared -- parse path unclear' }
          } finally { Remove-Item $md -Recurse -Force -EA SilentlyContinue }
          Write-Host "boot_chain_check.ps1 OK"
          exit 0

      - name: service_signature_check.ps1 (real services + Authenticode; svchost must resolve)
        shell: powershell
        run: |
          $ErrorActionPreference = 'Stop'
          $out = .\tools\service_signature_check.ps1
          $out | Select-Object -First 5 | Write-Host
          # The path parser must never misresolve well-known Windows services:
          # a "no-file" verdict for any svchost/system32 service means the
          # ImagePath normalization regressed (env vars, \SystemRoot\, unquoted
          # spaces). These services exist on every runner image.
          $bad = $out | Where-Object { $_ -match '\b(WinDefend|EventLog|Schedule|BITS|Dhcp|Dnscache)\b' -and $_ -match 'no-file' }
          if ($bad) { $bad | Write-Host; throw "service_signature_check misresolved a well-known service path (no-file)" }
          # -MarkerFile contract: the marker is written IFF at least one service
          # was flagged. Section 7 keys its FINDINGS raise on this file, so a
          # broken marker means a flagged malicious service reads CLEAN.
          $mk = Join-Path $env:TEMP 'dz_ci_svcgate.txt'
          Remove-Item $mk -Force -EA SilentlyContinue
          $out2 = .\tools\service_signature_check.ps1 -MarkerFile $mk
          $flagged = [bool](($out2 -join "`n") -match '\[WARNING\] \d+ service\(s\) failed')
          $marked  = Test-Path $mk
          Remove-Item $mk -Force -EA SilentlyContinue
          if ($flagged -ne $marked) { throw "service_signature_check -MarkerFile contract broken (flagged=$flagged marker=$marked)" }
          Write-Host "service_signature_check OK (marker contract: flagged=$flagged marker=$marked)"
          exit 0

      - name: persistence_eval.ps1 (Run-key + IFEO evaluation; clean host stays clean)
        shell: powershell
        run: |
          $ErrorActionPreference = 'Stop'
          # On a clean runner (no planted autoruns/IFEO) it must print the OK
          # line and must NOT false-positive on any pre-existing autorun.
          $out = .\tools\persistence_eval.ps1 -MarkerFile "$env:TEMP\dz_ci_persist.txt"
          $out | Write-Host
          if (Test-Path "$env:TEMP\dz_ci_persist.txt") {
            throw "persistence_eval flagged a finding on a clean runner (false positive): $($out -join '; ')"
          }
          if (-not ($out -match 'No suspicious Run-key autoruns or IFEO')) {
            throw "persistence_eval did not emit the clean-state OK line"
          }
          Write-Host "persistence_eval OK"
          exit 0

      - name: logon_persistence.ps1 (logon/unlock vectors; clean runner has no CRITICAL)
        shell: powershell
        run: |
          $ErrorActionPreference = 'Stop'
          Remove-Item "$env:TEMP\dz_logon_*.txt" -Force -EA SilentlyContinue
          $out = .\tools\logon_persistence.ps1
          $out | Write-Host
          # Must run cleanly and emit all section headers (Tier 1 + Tier 2).
          foreach ($h in 'Winlogon Notify', 'Network Provider', 'Credential Providers', 'LSA Notification', 'Screensaver', 'Logon script') {
            if (-not ($out -match [regex]::Escape($h))) { throw "logon_persistence missing section: $h" }
          }
          # A clean runner must not yield a CRITICAL logon-persistence finding.
          # (A WARNING for a legit non-Microsoft-signed provider is acceptable.)
          $crit = Get-ChildItem "$env:TEMP\dz_logon_*.txt" -EA SilentlyContinue |
            Where-Object { (Get-Content -LiteralPath $_.FullName -Raw) -match 'CRITICAL' }
          if ($crit) {
            Get-ChildItem "$env:TEMP\dz_logon_*.txt" | ForEach-Object { Write-Host ("{0}: {1}" -f $_.Name, (Get-Content -LiteralPath $_.FullName -Raw)) }
            throw "logon_persistence flagged a CRITICAL on a clean runner (false positive)"
          }
          Remove-Item "$env:TEMP\dz_logon_*.txt" -Force -EA SilentlyContinue

          # A BARE MODULE NAME IS NOT A MISSING DLL. Winlogon Notify DllName is
          # by design just a module name, which the loader resolves against
          # System32. Get-DllVerdict ran Test-Path on the raw value, resolved it
          # against the PowerShell working directory, failed, and returned
          # CRITICAL "DLL not found" -- a critical finding and a non-zero exit
          # on a healthy machine carrying a legitimate signed Notify handler.
          $nk = 'HKLM:\SOFTWARE\Microsoft\Windows NT\CurrentVersion\Winlogon\Notify\dz_ci_bare'
          New-Item -Path $nk -Force | Out-Null
          # A real, Microsoft-signed System32 DLL referenced the way the API
          # expects: bare name, no path.
          Set-ItemProperty -LiteralPath $nk -Name 'DllName' -Value 'wlnotify.dll' -Force
          if (-not (Test-Path (Join-Path ([Environment]::GetFolderPath('System')) 'wlnotify.dll'))) {
            Set-ItemProperty -LiteralPath $nk -Name 'DllName' -Value 'netlogon.dll' -Force
          }
          try {
            $out2 = (.\tools\logon_persistence.ps1) -join "`n"
            $ln = (($out2 -split "`n") | Where-Object { $_ -match 'dz_ci_bare' } | Select-Object -First 1)
            Write-Host $ln
            if (-not $ln) { throw 'planted Notify subkey was not reported at all' }
            if ($ln -match 'DLL not found') { throw ("a bare Notify DllName was misreported as a missing DLL: $ln") }
            if ($ln -match '^\[CRITICAL\]') { throw ("legitimate signed Notify handler reported CRITICAL (false positive): $ln") }
          } finally {
            Remove-Item -LiteralPath $nk -Recurse -Force -EA SilentlyContinue
            Remove-Item "$env:TEMP\dz_logon_*.txt" -Force -EA SilentlyContinue
          }
          Write-Host "logon_persistence OK (incl. bare Notify DllName resolution)"
          exit 0

      - name: INIT-path extractions (self_update_check / disk_info / smart_health)
        shell: powershell
        run: |
          $ErrorActionPreference = 'Stop'
          # self_update_check: an unreachable URL must hit the catch path,
          # emit an [INFO] line, and never throw.
          $u = .\tools\self_update_check.ps1 -LocalVer '7.2' -RemoteUrl 'http://127.0.0.1:0/version.txt' -DownloadUrl 'http://x/y.bat' | Out-String
          if ($u -notmatch '\[INFO\]|version') { Write-Host "self_update_check produced no status line"; exit 1 }
          # disk_info: each mode runs against real CIM / Get-PhysicalDisk.
          $vm = (.\tools\disk_info.ps1 -Mode VmFlag).Trim()
          if ($vm -ne 'yes_vm' -and $vm -ne 'no') { Write-Host "VmFlag unexpected: '$vm'"; exit 1 }
          $ssd = (.\tools\disk_info.ps1 -Mode SsdFlag).Trim()
          if ($ssd -ne 'yes_ssd' -and $ssd -ne 'no') { Write-Host "SsdFlag unexpected: '$ssd'"; exit 1 }
          .\tools\disk_info.ps1 -Mode VmReport | Out-Null
          .\tools\disk_info.ps1 -Mode DiskDetail | Out-Null
          $free = (.\tools\disk_info.ps1 -Mode FreeSpace -SystemDrive $env:SystemDrive | Out-String).Trim()
          if ($free -notmatch '^\d+$') { Write-Host "FreeSpace not numeric: '$free'"; exit 1 }
          # smart_health: report runs; flag must be warn/ok.
          .\tools\smart_health.ps1 -Mode Report | Out-Null
          $h = (.\tools\smart_health.ps1 -Mode Flag).Trim()
          if ($h -ne 'warn' -and $h -ne 'ok') { Write-Host "smart_health Flag unexpected: '$h'"; exit 1 }
          Write-Host "INIT-path extractions OK (self_update_check, disk_info x5, smart_health x2)"
          exit 0

      - name: dns_probe.ps1 (active DNS integrity probe, -dnsprobe)
        shell: powershell
        run: |
          $ErrorActionPreference = 'Stop'
          $marker = Join-Path $env:TEMP 'dz_ci_dnsprobe.txt'
          if (Test-Path $marker) { Remove-Item $marker -Force }
          $out = .\tools\dns_probe.ps1 -MarkerFile $marker | Out-String
          # Lenient on network variance: just require recognizable probe output
          # (per-domain '->' lines, or the all-clear / blackhole summary).
          if ($out -notmatch '->|security/update domains|did not resolve') {
            Write-Host "dns_probe produced no recognizable output:"; Write-Host $out; exit 1
          }
          Write-Host "dns_probe OK"
          Write-Host $out
          exit 0

      # Server runners have System Protection off, so Checkpoint-Computer
      # cannot create a real restore point. That's the point of THIS test:
      # in a no-op run, srp_check.ps1 must (1) execute without parser/runtime
      # errors and (2) NOT write the marker file. Writing it incorrectly is
      # exactly the trust-bug we're guarding against -- it'd cause doze_sec to
      # log a phantom "[CREATED]" entry in every change log.
      - name: srp_check.ps1 runs cleanly and does NOT mark on a no-op
        shell: powershell
        run: |
          $ErrorActionPreference = 'Stop'
          $m = Join-Path $env:TEMP 'dz_ci_srp_marker.txt'
          if (Test-Path $m) { Remove-Item $m -Force }
          .\tools\srp_check.ps1 -Description 'doze_sec CI selftest' -MarkerFile $m | Out-Null
          if (Test-Path $m) {
            Write-Host "srp_check FAILED: marker file was written despite no new restore point being created"
            Remove-Item $m -Force
            exit 1
          }
          Write-Host "srp_check OK (no marker on no-op)"
          exit 0

      # The INIT 12 if-exist block uses cmd.exe constructs (echo text>> file
      # inside a parenthesized if block). This step exercises it directly with
      # cmd.exe on a real Windows runner -- the only way to catch cmd.exe parse
      # errors like the "(if desired):" colon-after-paren crash that the PS-only
      # steps and full-run -nosrp both missed.
      - name: INIT 12 SRP if-exist changelog block (cmd.exe parse + run check)
        shell: cmd
        run: |
          rem Create marker so the if-exist condition fires
          echo.> "%TEMP%\dz_srp_created.txt"
          set "CHANGELOG=%TEMP%\dz_ci_srp_changelog.txt"
          set "SCRIPT_VERSION=7.3"
          if exist "%TEMP%\dz_srp_created.txt" (
              echo [CREATED] System Restore Point: "Pre-WIN11-Security-Audit-v%SCRIPT_VERSION%">> "%CHANGELOG%"
              echo           This is a safety net -- it lets you roll back any changes made AFTER this point.>> "%CHANGELOG%"
              echo           Undo ^(if desired^): Control Panel ^> System ^> System Protection ^> System Restore>> "%CHANGELOG%"
              echo           Select the restore point named Pre-WIN11-Security-Audit-v%SCRIPT_VERSION%>> "%CHANGELOG%"
              echo           NOTE: This is intentional and recommended. Only remove it if you are certain.>> "%CHANGELOG%"
              del "%TEMP%\dz_srp_created.txt" 2>nul
          )
          if exist "%TEMP%\dz_srp_created.txt" ( echo FAILED: marker not deleted & exit /b 1 )
          if not exist "%CHANGELOG%" ( echo FAILED: changelog not written & exit /b 1 )
          findstr /c:"CREATED" "%CHANGELOG%" >nul || ( echo FAILED: CREATED line missing & exit /b 1 )
          findstr /c:"if desired" "%CHANGELOG%" >nul || ( echo FAILED: if-desired line missing & exit /b 1 )
          echo INIT 12 if-exist block OK ^(cmd.exe parse and run^)
          exit /b 0

      # Section 4 suspicious-path patterns cross the cmd -> powershell.exe -File
      # argv boundary. A trailing UNdoubled backslash before the closing quote
      # ("\Temp\") is parsed by .NET argv rules as an escaped literal quote,
      # fusing the patterns into one garbage string -- a silent false negative
      # for the flagship malware-in-Temp check. This step runs the EXACT bat
      # invocation (doubled backslashes) from cmd and asserts both a hit and a
      # miss, so any regression in the quoting breaks CI instead of the check.
      - name: select_lines.ps1 argv quoting (Temp/AppData patterns match from cmd)
        shell: cmd
        run: |
          echo evil.exe  666  C:\Users\x\AppData\Local\Temp\evil.exe> "%TEMP%\dz_ci_argv.tmp"
          powershell -NoProfile -ExecutionPolicy Bypass -File tools\select_lines.ps1 -Path "%TEMP%\dz_ci_argv.tmp" "\Temp\\" "\AppData\\" "\Downloads\\" "\Recycle" "\Users\Public" >nul
          if not "%ERRORLEVEL%"=="0" ( echo FAILED: \Temp\ path did not match -- argv quoting regressed & exit /b 1 )
          echo winword.exe  777  C:\Program Files\Microsoft Office\winword.exe> "%TEMP%\dz_ci_argv.tmp"
          powershell -NoProfile -ExecutionPolicy Bypass -File tools\select_lines.ps1 -Path "%TEMP%\dz_ci_argv.tmp" "\Temp\\" "\AppData\\" "\Downloads\\" "\Recycle" "\Users\Public" >nul
          if not "%ERRORLEVEL%"=="1" ( echo FAILED: benign path matched -- patterns are corrupt & exit /b 1 )
          echo select_lines argv quoting OK ^(hit on \Temp\, miss on benign^)
          exit /b 0

      # Guard the bat files themselves: the suspicious-path call sites must
      # keep the doubled-backslash form, and BOTH 18e scheduled-task IOC
      # matchers must keep [regex]::Escape (the fix was once applied to only
      # one of the two scripts, silently killing 6 backslash-path IOCs in the
      # other).
      - name: bat call sites keep argv/regex fixes in BOTH scripts
        shell: powershell
        run: |
          $ErrorActionPreference = 'Stop'
          foreach ($f in 'doze_sec.bat','doze_sec_noAdmin.bat') {
            if (-not (Select-String -Path $f -SimpleMatch '"\Temp\\" "\AppData\\" "\Downloads\\"' -Quiet)) {
              Write-Host "FAILED: $f lost the doubled-backslash suspicious-path patterns"; exit 1
            }
            if (-not (Select-String -Path $f -SimpleMatch '[regex]::Escape($p.Trim())' -Quiet)) {
              Write-Host "FAILED: $f lost [regex]::Escape in the 18e scheduled-task IOC matcher"; exit 1
            }
          }
          # Every active scheduled-task IOC must be usable as an escaped literal.
          $bad = @(Get-Content ThreatLists\ioc_scheduled_tasks.txt | Where-Object { $_ -and $_ -notmatch '^\s*#' } | Where-Object {
            try { [void]('probe' -match [regex]::Escape($_.Trim())); $false } catch { $true }
          })
          if ($bad) { Write-Host "FAILED: unusable IOC pattern(s): $($bad -join ', ')"; exit 1 }
          # The end-of-run CRITICAL escalation must use the resolved %PWSH%, not
          # bare 'powershell' -- on a host where powershell is not on PATH (the
          # exact case %PWSH% resolution exists for) a bare call no-ops and exit
          # code 8 is never raised. Assert the scrape line uses "%PWSH%".
          foreach ($f in 'doze_sec.bat','doze_sec_noAdmin.bat') {
            $scrape = Select-String -Path $f -SimpleMatch "Select-String -LiteralPath '%REPORT%' -Pattern '\A\[CRITICAL\]'"
            if (-not $scrape) { Write-Host "FAILED: $f lost the CRITICAL-escalation scrape line"; exit 1 }
            if ($scrape.Line -notmatch '"%PWSH%"') {
              Write-Host "FAILED: $f CRITICAL-escalation scrape uses bare powershell, not %PWSH% -- $($scrape.Line.Trim())"; exit 1
            }
          }
          Write-Host 'argv/regex fix guards OK (both bats, all task IOCs escape-safe, CRIT scrape uses %PWSH%)'
          exit 0

      # vt_self_check.ps1 is called from the bats as -Binaries "a","b","c".
      # powershell.exe -File binds that as ONE comma-joined string, which the
      # script must split -- before this was fixed, zero binaries resolved and
      # the VT integrity gate silently verified nothing. Run the EXACT bat-style
      # call from cmd with a dummy token and assert all four binaries resolve
      # (the dummy key then 401s and exits 2, which is fine -- resolution
      # happens before any network call).
      - name: vt_self_check.ps1 resolves comma-bound -Binaries from cmd
        shell: cmd
        run: |
          mkdir "%TEMP%\dz_vt_home" 2>nul
          echo dummy-token-for-binding-test> "%TEMP%\dz_vt_home\.vt_token"
          set "USERPROFILE=%TEMP%\dz_vt_home"
          powershell -NoProfile -ExecutionPolicy Bypass -File tools\vt_self_check.ps1 -SleepSeconds 0 -TimeoutSeconds 5 -Binaries "%SystemRoot%\System32\WindowsPowerShell\v1.0\powershell.exe","%SystemRoot%\System32\wevtutil.exe","%SystemRoot%\System32\reg.exe","%SystemRoot%\System32\findstr.exe"> "%TEMP%\dz_vt_out.txt" 2>&1
          findstr /c:"no resolvable binaries" "%TEMP%\dz_vt_out.txt" >nul && ( echo FAILED: comma-bound -Binaries did not split -- VT gate is dead again & type "%TEMP%\dz_vt_out.txt" & exit /b 1 )
          findstr /c:"verifying 4" "%TEMP%\dz_vt_out.txt" >nul || ( echo FAILED: expected all 4 binaries to resolve & type "%TEMP%\dz_vt_out.txt" & exit /b 1 )
          echo vt_self_check comma-binding OK ^(4/4 binaries resolved^)
          exit /b 0

  full-run:
    name: "doze_sec.bat end-to-end (hang guard, hard timeout)"
    runs-on: windows-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4

      - name: Run the full audit; fail on hang (timeout), fatal error, or early exit
        shell: cmd
        run: |
          call doze_sec.bat -dev -sdu -nosrp -resetTTP -dnsprobe
          set RC=%ERRORLEVEL%
          echo doze_sec.bat exit code: %RC%
          if "%RC%"=="1" ( echo FATAL ERROR exit code 1 & exit /b 1 )
          set "REP="
          for %%R in ("C:\SecurityAudit\SecurityReport_*.txt") do set "REP=%%R"
          if not defined REP ( echo NO REPORT PRODUCED & exit /b 1 )
          rem A hang is caught by the job timeout; this catches an EARLY EXIT
          rem (e.g. a parse error mid-audit) that still left a partial report:
          rem the exit handler's "EXIT CODE:" line and the [18/18] banner only
          rem exist if the audit actually ran to the end.
          findstr /c:"EXIT CODE:" "%REP%" >nul || ( echo AUDIT DID NOT REACH EXIT HANDLER -- early exit or crash & exit /b 1 )
          findstr /c:"[18/18]" "%REP%" >nul || ( echo AUDIT DID NOT REACH SECTION 18 -- early exit & exit /b 1 )
          rem A REAL run (no -selftest) must never carry the test banner nor land
          rem in the selftest quarantine; the harness jobs assert the inverse.
          findstr /c:"TEST RUN -- every finding below was planted" "%REP%" >nul && ( echo REAL RUN CARRIES THE TEST BANNER & exit /b 1 )
          echo %REP% | findstr /i /c:"\selftest\" >nul && ( echo REAL RUN LANDED IN THE SELFTEST QUARANTINE & exit /b 1 )
          echo Completed end-to-end through Section 18 and the exit handler.
          exit /b 0

      - name: Upload the audit artifacts for inspection
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: securityaudit-output
          path: C:\SecurityAudit\**
          if-no-files-found: ignore

  # Read-only field test. full-run above proves the audit completes; this
  # proves the -readonly promise on a real Windows machine using the exact
  # script a person runs on their own machine (tests\field_test.ps1): the
  # RunOnce key, the boot configuration and the restore-point count are
  # captured before and after and must be unchanged; the report must carry
  # the READ-ONLY banner and declare every skipped step; and the benign
  # look-alike corpus must grade it with no known false positive returning.
  readonly-field-test:
    name: "tests/field_test.ps1: -readonly changes nothing on the machine (proven before/after)"
    runs-on: windows-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4

      - name: Run the field test (read-only audit + read-only proof + corpus check)
        shell: powershell
        run: |
          $ErrorActionPreference = 'Stop'
          $out = (& .\tests\field_test.ps1 -BatPath .\doze_sec.bat -NoConsoleLog *>&1 | Out-String)
          $rc = $LASTEXITCODE
          Write-Host $out
          Write-Host ("field_test.ps1 exit code: {0}" -f $rc)
          if ($rc -ne 0) { throw "field_test.ps1 failed (see above)" }
          # The proof lines must have actually executed -- not skipped -- on this elevated runner.
          foreach ($must in @('RunOnce resume key: absent after the run', 'boot configuration (bcdedit /enum {bootmgr}): unchanged',
                              'report carries the READ-ONLY banner', 'report carries no TEST RUN banner',
                              'read-only skip declared: boot menu left as found', 'no known benign look-alike is reported above')) {
            if ($out -notmatch [regex]::Escape($must)) { throw ("field_test output lacks the proof line: {0}" -f $must) }
          }
          if ($out -match '\[FAIL') { throw 'field_test printed a FAIL line' }
          # Independent check, not trusting the script under test: the key is really absent.
          $ro = Get-ItemProperty -LiteralPath 'HKCU:\Software\Microsoft\Windows\CurrentVersion\RunOnce' -EA SilentlyContinue
          if ($ro -and ($ro.PSObject.Properties.Name -contains '*doze_sec_resume')) { throw 'RunOnce resume value exists after a -readonly run' }
          Write-Host 'read-only field test: proven on this runner.'

      - name: Upload the audit artifacts for inspection
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: readonly-field-test-output
          path: C:\SecurityAudit\**
          if-no-files-found: ignore

  # Detection-regression harness. full-run above proves the audit COMPLETES;
  # this proves it DETECTS. It plants known-bad artifacts (WDigest=1, a HOSTS
  # hijack, a Run-key backdoor, an IFEO Debugger on notepad, an enabled Guest
  # account), runs the audit, and asserts the report flags them. Every case is
  # "required": a regression fails the job. (The "pending" tier this comment
  # used to describe is empty -- every gap the original code review found was
  # promoted on 2026-07-18 -- so nothing here is exempt from failing.)
  #
  # A plant that FAILS also fails the job unless the case declares MaySkip with
  # a reason. A silently skipped case is a detection that stopped being tested,
  # and a green job would then mean "checked nothing here" while looking
  # identical to "checked and fine".
  #
  # Every artifact is removed in a finally block.
  detection-selftest:
    name: "detection regression harness (plant known-bad -> assert verdict)"
    runs-on: windows-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4

      - name: Plant indicators, run the audit, assert detections, clean up
        shell: powershell
        run: |
          $ErrorActionPreference = 'Stop'
          .\tests\detection_selftest.ps1 -BatPath .\doze_sec.bat
          exit $LASTEXITCODE

      # A field test on a WSL machine got a credential-capture WARNING for
      # Microsoft's own P9NP provider (p9np.dll): the netprov branch upgraded
      # Microsoft-signed-System32 verdicts to WARNING to keep a pre-WSL
      # three-name allowlist authoritative. Pin the fix: a provider whose DLL
      # is a Microsoft-signed System32 binary must be [INFO] with no marker.
      # (The true-positive side is already pinned by detection_selftest.ps1,
      # which plants dz_selftest_np with a staging-path DLL and requires the
      # CRITICAL.) Testable at all only because this PR gave the tool a
      # -MarkerDir parameter.
      - name: "Network provider: Microsoft-signed System32 provider is context, not a finding"
        shell: powershell
        run: |
          $ErrorActionPreference = 'Stop'
          $npOrder = 'HKLM:\SYSTEM\CurrentControlSet\Control\NetworkProvider\Order'
          $prev = (Get-ItemProperty -Path $npOrder -Name ProviderOrder).ProviderOrder
          $svc = 'HKLM:\SYSTEM\CurrentControlSet\Services\dz_ci_msnp\NetworkProvider'
          $md = Join-Path $env:TEMP ("dz_np_ci_{0}" -f $PID)
          try {
            Set-ItemProperty -Path $npOrder -Name ProviderOrder -Value ($prev + ',dz_ci_msnp')
            New-Item -Path $svc -Force | Out-Null
            Set-ItemProperty -Path $svc -Name ProviderPath -Value 'C:\Windows\System32\ntlanman.dll'
            $out = (.\tools\logon_persistence.ps1 -MarkerDir $md) -join "`n"
            if (-not $out) { throw 'logon_persistence produced no output -- nothing was tested' }
            $npLines = ($out -split "`n" | Where-Object { $_ -match 'dz_ci_msnp' }) -join "`n"
            Write-Host $npLines
            if ($npLines -notmatch '\[INFO\]') { throw 'a Microsoft-signed System32 provider was not reported as INFO (the WSL/P9NP false positive)' }
            if ($npLines -match '\[WARNING\]|\[CRITICAL\]') { throw 'a Microsoft-signed System32 provider was flagged as a finding' }
            if (Test-Path (Join-Path $md 'dz_logon_netprov.txt')) { throw 'a Microsoft-signed System32 provider raised the netprov marker' }
            Write-Host 'Microsoft-signed provider correctly reported as context'
          } finally {
            Set-ItemProperty -Path $npOrder -Name ProviderOrder -Value $prev
            Remove-Item 'HKLM:\SYSTEM\CurrentControlSet\Services\dz_ci_msnp' -Recurse -Force -EA SilentlyContinue
            Remove-Item $md -Recurse -Force -EA SilentlyContinue
          }

      # A real machine's report (field test, 2026-08-30) flagged "System event
      # log was cleared" off a smart-card-reader driver's Event 104: event IDs
      # are unique only PER PROVIDER, and the query matched the bare ID. This
      # pins the provider-filtered query in BOTH directions, and tests the
      # SHIPPED fragment extracted from doze_sec.bat -- not a copy that can
      # drift. Runs after the main harness because direction (b) clears this
      # ephemeral runner's System log.
      - name: "Event 104 provider filter: benign 104 stays quiet, a real clear fires"
        shell: powershell
        run: |
          $ErrorActionPreference = 'Stop'
          $line = (Select-String -Path .\doze_sec.bat -Pattern '^echo .*dz_ev104_hit' | Select-Object -First 1).Line
          if (-not $line) { throw 'could not find the ev104 fragment in doze_sec.bat' }
          $lineNA = (Select-String -Path .\doze_sec_noAdmin.bat -Pattern '^echo .*dz_ev104_hit' | Select-Object -First 1).Line
          if ($lineNA -ne $line) { throw 'ev104 fragment differs between doze_sec.bat and doze_sec_noAdmin.bat' }
          foreach ($bat in '.\doze_sec.bat','.\doze_sec_noAdmin.bat') {
            $l1102 = (Select-String -Path $bat -Pattern '^echo .*dz_ev1102_hit' | Select-Object -First 1).Line
            if ($l1102 -notmatch "ProviderName='Microsoft-Windows-Eventlog'") { throw "$bat 1102 query lost its provider filter" }
          }
          $frag = $line -replace '^\s*echo ', '' -replace ' >> "%PSRUN%"\s*$', ''
          if ($frag -notmatch "ProviderName='Microsoft-Windows-Eventlog'") { throw 'the ev104 query lost its provider filter' }
          $marker = Join-Path $env:TEMP 'dz_ev104_hit.txt'

          # (a) FALSE-POSITIVE direction -- the field report's exact shape: a
          # non-eventlog provider's Event 104 in the System log must not be
          # what the check matches. This CANNOT be asserted on the fragment's
          # whole output: the runner image already carries a genuine
          # Microsoft-Windows-Eventlog 104, and 'wevtutil cl' writes another
          # into the fresh log, so no reachable state is free of genuine
          # clears -- the first version of this step silently skipped here and
          # then claimed "both directions". Test the discriminating PROPERTY
          # instead, using the provider name taken from the SHIPPED fragment:
          # the bare-ID query (the field bug) must match the planted benign
          # event, and the shipped provider-filtered query must exclude it.
          # Holds on any image; no skip path.
          $prov = ([regex]::Match($frag, "ProviderName='([^']+)'")).Groups[1].Value
          if (-not $prov) { throw 'could not extract the provider filter from the shipped ev104 fragment' }
          if (-not [System.Diagnostics.EventLog]::SourceExists('dz_ci_benign104')) {
            New-EventLog -LogName System -Source dz_ci_benign104
          }
          Write-EventLog -LogName System -Source dz_ci_benign104 -EventId 104 -EntryType Information -Message 'benign 104: smart-card-reader-shaped noise, not a log clear'
          Start-Sleep -Seconds 2
          $bare = @(Get-WinEvent -FilterHashtable @{LogName='System';Id=104} -EA SilentlyContinue | Where-Object { $_.ProviderName -eq 'dz_ci_benign104' })
          if ($bare.Count -lt 1) { throw 'benign 104 did not land in the System log -- direction (a) could not be tested (this is a failure, not a skip)' }
          Write-Host ("direction (a): bare-ID query matches the planted benign 104 ({0} event(s)) -- the field bug shape reproduces" -f $bare.Count)
          $filtered = @(Get-WinEvent -FilterHashtable @{LogName='System';Id=104;ProviderName=$prov} -EA SilentlyContinue)
          $leak = @($filtered | Where-Object { $_.ProviderName -ne $prov })
          if ($leak.Count -gt 0) { throw ("the shipped provider filter let {0} non-{1} event(s) through -- the field false positive is NOT fixed" -f $leak.Count, $prov) }
          Write-Host ("direction (a): shipped filter ({0}) returns {1} event(s), zero from the benign source -- discrimination PASS" -f $prov, $filtered.Count)
          Remove-Item $marker -Force -EA SilentlyContinue

          # (b) TRUE-POSITIVE direction, never previously proven: actually
          # clear this ephemeral runner's System log. The clear itself writes
          # 104 from Microsoft-Windows-Eventlog into the fresh log.
          wevtutil cl System
          Start-Sleep -Seconds 3
          Remove-Item $marker -Force -EA SilentlyContinue
          $out2 = Invoke-Expression $frag | Out-String
          Write-Host $out2.Trim()
          if ($out2 -notmatch '\[WARNING\]') { throw 'a genuinely cleared System log was NOT detected' }
          if (-not (Test-Path $marker)) { throw 'cleared-log WARNING wrote no marker -- the finding would never reach the ledger' }
          Write-Host 'ev104: benign-source discrimination PASS; genuine-clear detection PASS'

      - name: Upload the selftest audit artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: detection-selftest-output
          path: C:\SecurityAudit\**
          if-no-files-found: ignore

  # The same plant/assert harness against doze_sec_noAdmin.bat, elevated.
  # Elevated, the adaptive bat sets IS_ADMIN=1 and runs its full path, so every
  # detection, section verdict, and Option B consistency assertion must hold
  # exactly as they do for doze_sec.bat -- this is the mirror-parity check the
  # design memo (section 7) called out as missing.
  detection-selftest-noadmin:
    name: "detection regression harness (doze_sec_noAdmin.bat, elevated adaptive path)"
    runs-on: windows-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4

      - name: Plant indicators, run the noAdmin bat elevated, assert detections
        shell: powershell
        run: |
          $ErrorActionPreference = 'Stop'
          .\tests\detection_selftest.ps1 -BatPath .\doze_sec_noAdmin.bat
          exit $LASTEXITCODE

      - name: Upload the selftest audit artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: detection-selftest-noadmin-output
          path: C:\SecurityAudit\**
          if-no-files-found: ignore

  # The path CI never executed before: doze_sec_noAdmin.bat as a REAL standard
  # user (created on the runner, launched via Start-Process -Credential).
  # Asserts the non-admin contract: report says Admin : 0, sections defer with
  # PARTIAL verdicts, a planted HKLM CRITICAL is still detected without admin,
  # exit codes 6/8 honor their semantics, and the Option B ledger consistency
  # net holds under deferral. See tests/noadmin_smoke.ps1.
  noadmin-true-run:
    name: "doze_sec_noAdmin.bat as a real standard user (deferral contract)"
    runs-on: windows-latest
    timeout-minutes: 35
    steps:
      - uses: actions/checkout@v4

      - name: Create standard user, run the audit twice, assert the contract
        shell: powershell
        run: |
          $ErrorActionPreference = 'Stop'
          .\tests\noadmin_smoke.ps1 -BatPath .\doze_sec_noAdmin.bat
          exit $LASTEXITCODE

      - name: Upload the non-admin audit artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: noadmin-smoke-output
          path: noadmin-smoke-output\*
          if-no-files-found: ignore
